# 1.2.0 · P1 — the rules file as a map

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use `- [ ]`. **Coder, tester and task lead never commit** — the last step of every task is the session's commit, after it has accepted the task (P4: `check` on the report, three pointers opened, the full suite run by the session itself).

**Revision:** re-scoped 2026-09-23 to the rewritten spec section (`6951a5e`), ceiling 13 100 per the session's ruling (`917f047`): P1 rewrites no rule. The earlier revisions of this plan (one line per rule, fix rounds 1 and 2) are withdrawn in full.

**Goal:** The rendered rules file loses the prose that explains how the handoff hooks work and keeps every rule byte for byte. The three hook-mechanics bullets of "Ledger and handoff" become their five obligations — verbatim, one line each — plus one sentence on what `rules --hooks` wires, pointing at a new `## The handoff hooks` section of `docs/CONFIG.md`. Rendered size: 14 390 → 12 521 characters, pinned at ≤ 13 100.

**Architecture:** Text only — no `.mjs` outside `test/` changes; `core/rules.mjs`, `docs/ORCHESTRATION.md`, the agent definitions and the skill are untouched. Task 1 writes the destination (CONFIG) and creates `test/rules-size.test.mjs`; Task 2 replaces template lines 29–31 and pins the result; Task 3 records the before/after row in MEASUREMENTS. Every other line of the template stays as it is, which Step 2.5 checks with `git diff`.

**Tech Stack:** Node ≥ 20, zero runtime dependencies, ESM, `node --test test/` (`npm test`).

**Spec:** `docs/superpowers/specs/2026-09-20-1.2.0-context-design.md` at `917f047` — **"P1 — the rules file as a map"** (rewritten 2026-09-23), "Tests" (the `rules-size` and `docs-map` lines), and the P1 row of "Rows owed to MEASUREMENTS". "P2" read for context: it adds three rules to the same template and a subsection to ORCHESTRATION "Evidence with pointers"; nothing here touches either.

**Scout map:** `.omelette/map-1.2.0.md`, `commit: 8f46bc3`. `omelette-fleet check` on it: 210 pointers, 210 ok. Spot-checked three pointers by hand — `rules/omelette-fleet.md:29` (`After a compaction, re-read the ledger before doing anything else.`), `core/rules.mjs:256` (`export function renderRulesFile(version, workflow = {}) {`), `test/review-0.3.7-fixes.test.mjs:598` (`assert.ok(orchestration.includes(bullet.slice(2)), `) — all three true, so the map is treated as verified. Commits since the map (`1c412cf`…`6951a5e`: P0's script, tests and MEASUREMENTS rows; two spec edits) touch none of the files this plan edits except MEASUREMENTS, whose anchors were re-checked at `6951a5e`.

| Map lines | Count | How this plan used them |
|---|---:|---|
| `rules/omelette-fleet.md — headings` (all) and `— obligations, one per bullet` (all) | 60 | **Re-taken**: the template was read whole. The inventory's lines 29–31 are the three bullets Task 2 replaces; the five obligations are quoted from them |
| `core/rules.mjs` :60, :61, :75, :92, :95, :117, :127, :243, :256, :263–:267, :271, :295, :657, :670 | 18 | **Re-taken** (read 55–130, 240–300, 650–700): rendering, marker, merge sentence |
| `core/rules.mjs` :155, :180, :208, :210–:214, :228, :385, :431, :477, :488, :594, :632; `core/unit.mjs` :544, :566; `core/config.mjs:187`; `bin/omelette-fleet.mjs:2629` | 19 | **Relied on** as given: the short contract depends on the rules file saying what the full contract says — untouched here, since no rule changes |
| `Tests that pin the rules text` (all) | 34 | **Re-taken**: every file opened at the cited lines. None pins text of lines 29–31 |
| `test/docs-map.test.mjs` (all) | 10 | **Re-taken**: read whole |
| `docs/CONFIG.md` (all) | 9 | **Re-taken**: read 1–175 and every heading |
| `docs/ORCHESTRATION.md` (all) | 20 | **Re-taken**: read whole. Not edited by this plan |
| `hooks/omelette-guard.mjs` :481, :519, :587, :636, :639, :691, :725, :726, :758, :759, :817, :1035, :1227, :1257, :1261, :1314, :1321, :1337, :1500, :1501, :1512, :1522 | 22 | **Re-taken**: every fact the new CONFIG section states was read off the guard |
| `hooks/omelette-guard.mjs` :510, :1006; `core/config.mjs:249` | 3 | **Relied on**: the CONFIG section links to "Handoff settings" for the window instead of restating the resolver |
| `agents/`, `skills/`, `package.json`, `CHANGELOG.md` | 15 | Not needed |

Relied on without re-opening: **22** lines. Re-taken: **173** lines. Beyond the map, every reader of the rules template, CONFIG and MEASUREMENTS in `test/` was grepped: no test pins the text of template lines 29–31; `test/rules.test.mjs:765` (the `session` render never says `pull request`) and the evidence, lane and merge pins all read lines this plan does not touch.

## Size budget

| Rendered rules file (`renderRulesFile(v, { merge })`) | `session`, characters | `pr`, characters | `session`, UTF-8 bytes |
|---|---:|---:|---:|
| 1.1.0 (= HEAD before this package) | 14 390 | 14 358 | 14 479 |
| **after Task 2 (this package's result)** | **12 521** | **12 489** | **12 592** |
| ceiling pinned by Task 2 (spec) | 13 100 | 13 100 | — |
| left for P2's three rules | **579** | 611 | — |

The five obligations with no wiring sentence at all render 12 344; the wiring sentence, with the opt-in condition and the name of the CONFIG section, adds 177. P2's three rules have 579 characters (`session`, the binding policy).

## Rulings on the spec

1. **"13 100 characters rendered" is `text.length` of `renderRulesFile(version, { merge })`, for both merge policies.** The rendered 1.1.0 file is 14 390 characters / 14 479 bytes; the test measures characters (the spec's word); MEASUREMENTS reports both. No character in the file is outside the BMP, so `length` is also the code-point count.
2. **The wiring sentence and the ceiling — ruled by the session.** The spec's first ceiling, 12 400, left room for a 51-character wiring line only, no opt-in condition and 2 characters for P2. The session ruled: the ceiling is **13 100** (spec `917f047`), and the wiring line keeps the opt-in condition and names the CONFIG section:

   ~~~md
   - `omelette-fleet rules --hooks` wires the ledger hooks, which say nothing unless `.omelette/` holds a `ledger-*.md`; how each one works: `docs/CONFIG.md`, "The handoff hooks".
   ~~~

   It renders 12 521 (`session`) / 12 489 (`pr`), leaving P2 579 / 611.
3. **Verbatim.** The five obligations are the 1.1.0 sentences exactly: obligations 1 and 4 keep their bold as today; "the handoff block is still yours to write" and "a ledger kept in another repository gets neither the stamp nor the print" opened mid-bullet in 1.1.0 and gain their sentence-initial capital — the only difference, and the test pins the resulting lines whole. Obligation 3 is today's full sentence, "The discipline is unchanged: handoffs at every natural pause, and the hook is the net under it."
4. **What else the three bullets said, and where it is now.** All of it is in CONFIG "The handoff hooks" (Task 1): the stamp line and that it lands announced or not; the print and its bounds (40 lines / 4 KB per ledger, 12 KB in all); that both read `<session cwd>/.omelette/ledger-*.md`; the summary heading, its 8 KB bound and why the print and the gate ignore it; `handoff.compactSummary=false`; the threshold, the window (linked to "Handoff settings", which holds the resolver chain) and the once-per-crossing reminder and gate; the opt-in, which the rules also keep, in the wiring sentence. "The discipline is still the file, not the hook: what the hook prints is the handoff block you wrote" is carried by obligations 2 and 3 in the rules and by "they print only the handoff block you wrote" in CONFIG.
5. **"Each its own sentence"** is built as each its own bullet line, and the test asserts `- <sentence>` as a whole line for each of the five.
6. **Where `## The handoff hooks` goes in CONFIG**: a level-2 section directly before `## Keys` — placed after `### Handoff settings` it would re-parent `### Workflow settings`. `### Handoff settings` gains one link to it; the map gains a row after the Handoff-settings row.
7. **ORCHESTRATION is not edited.** Its small-change copy stays byte-identical to the template (the 0.3.7 test holds untouched), and its own hook prose ("Layer 3", "Ledger and handoff" line 129) overlaps the new CONFIG section — a docs-pass question, not P1's.
8. **"Every other line byte for byte" is checked by `git diff` in Step 2.5, not by a test.** A golden hash of the template would also pin it, but every later rules change (P2's first) would have to re-bless the hash; the diff check at build time is the spec's claim, verified once.
9. **The P1 MEASUREMENTS row is a new section** "The rules file, before and after", next to "The fleet contract, full and short" (both are resident in every session), with a map row and a "How the numbers are taken" bullet. Task 3 has a precondition so it never commits another package's rows.

## Global Constraints

- Node ≥ 20, zero runtime dependencies. **No `.mjs` file outside `test/` changes**; `core/rules.mjs`, `hooks/omelette-guard.mjs`, `agents/*.md`, `skills/**`, `docs/ORCHESTRATION.md` are untouched. `units/gemini/models.js` is never edited.
- **The template changes in lines 29–31 only** (three lines out, six in). No other bullet, heading, table row or sentence is shortened, reworded or moved.
- `npm test` (= `node --test test/`) ends `# fail 0` before every commit. The session runs it itself before it commits (P4 acceptance).
- The template keeps `{{marker}}` on line 1 and `{{merge}}` exactly once; it never contains `pull request` or `merges the branch into main itself` (pinned by `test/rules.test.mjs:761–790`).
- Every doc keeps its `| Question | Where |` map passing `test/docs-map.test.mjs`: a new heading gets a map row in the same commit.
- Public repository: no absolute home path in any committed file; commands below say `<repo root>`.
- Branch `feat/1.2.0`. Ledger `.omelette/ledger-1.2.0.md` (gitignored): one line per task with its commit.
- Commits: one task, one commit, explicit `git add <paths>`, subject `<area>: <what changed for the reader>`, body = why, trailers exactly:
  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01854UFRDMB6pEz5bRWeYMhE
  ```
- The rendered copies under this checkout's `.claude/` are gitignored: refreshed once after Task 2 (Step 2.7), never committed.
- CHANGELOG is not this package's.

## How to read the code blocks

Every fenced block below is verbatim — nothing is indented for the list around it. Markdown content is fenced with `~~~md` so the backticks inside it survive. An edit is given as an **exact old text** and an **exact new text**; line numbers are locators at `6951a5e`, not addresses.

## File map

| File | Task | What happens |
|---|---|---|
| `docs/CONFIG.md` | 1 | map row after line 11; one sentence appended to line 125; new `## The handoff hooks` before `## Keys` (line 172) |
| `test/rules-size.test.mjs` | 1, 2 | created in Task 1 (helpers, `MECHANICS`, the CONFIG test); three tests appended in Task 2 |
| `rules/omelette-fleet.md` | 2 | lines 29–31 → six lines; nothing else |
| `docs/MEASUREMENTS.md` | 3 | map row after line 10; section after line 71; bullet after line 142 (numbering at `6951a5e`) |

## Pinned text: every existing pin is kept

No existing test reads template lines 29–31, so no pin is updated. Checked: `test/evidence-pointers.test.mjs:29–42` (the two evidence bullets, not touched); `test/rules.test.mjs:141–169` (marker, placeholders, `When a delegate is justified`, `^## Reviews$`, `review yield:`), `:761–790` (merge sentences), `:792–808` (the small-change lane, exception before lane before `## Ledger and handoff`); `test/rules-tester.test.mjs:22–33, 94–`; `test/tester-0.3.4-p2-review.test.mjs:150–155`; `test/tester-0.3.7-m5-m6-extra.test.mjs:136–167`; `test/review-0.3.7-fixes.test.mjs:592–604` (the lane bullets verbatim in ORCHESTRATION — neither file's lane text changes); `test/check.test.mjs:743`; `test/tester-1.1.0-check-review.test.mjs:268`.

---

### Task 1: CONFIG — the handoff hooks get a section of their own

**Files:**
- Create `test/rules-size.test.mjs`.
- Modify `docs/CONFIG.md`: after line 11 (map row); line 125 (one sentence appended); before line 172 `## Keys` (new section).

**Interfaces**
- *Consumes:* nothing of code. Facts from `hooks/omelette-guard.mjs` (`preCompact` 636–646, `sessionStart` 758–, `lastHandoffBlock` 691 `maxLines = 40, maxBytes = 4096`, `HANDOFF_TOTAL_MAX` 726, `inSubagent` 817, `handoffContext` 1227, `postToolUse` 1257, `stop` 1314, `SUMMARY_MAX` 1337, `postCompact` 1500) — the section states only what those lines do.
- *Produces:* heading `## The handoff hooks`, anchor `#the-handoff-hooks` (Task 2's rules sentence points at `docs/CONFIG.md`; Task 3's MEASUREMENTS section names it); the helpers `read(rel)`, `rules()`, `section(md, heading)` and the list `MECHANICS` in `test/rules-size.test.mjs`, used by Task 2:
  ```js
  const read = (rel) => string;                       // a repository file, utf8
  const rules = () => string;                         // renderRulesFile('1.2.0', { merge: 'session' })
  function section(md, heading) → string;             // heading line to the next heading of the same or higher level
  const MECHANICS = string[];                         // the hook-mechanics strings: in CONFIG's section, never in the rules
  ```

- [ ] **Step 1.1 — write the failing test.** Create `test/rules-size.test.mjs` with exactly:

```js
// 1.2.0 P1 (spec docs/superpowers/specs/2026-09-20-1.2.0-context-design.md,
// "P1 — the rules file as a map"): the rules file keeps every rule byte for byte
// and loses only the prose that explained how the handoff hooks work, which now
// lives in CONFIG "The handoff hooks". These tests pin both halves: what left the
// rules, and where it went.
import { test } from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { renderRulesFile } from '../core/rules.mjs';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const read = (rel) => fs.readFileSync(path.join(ROOT, rel), 'utf8');
/** The rules file as a session loads it: rendered, under the default merge policy. */
const rules = () => renderRulesFile('1.2.0', { merge: 'session' });

/** One section of a Markdown text: its heading line up to the next heading of the same or a higher level. */
function section(md, heading) {
  const from = md.indexOf(`\n${heading}\n`);
  assert.notEqual(from, -1, `${heading} present`);
  const level = heading.match(/^#+/)[0].length;
  const next = new RegExp(`\\n#{1,${level}} `, 'g');
  next.lastIndex = from + 1;
  const m = next.exec(md);
  return md.slice(from, m ? m.index : undefined);
}

/** How the handoff hooks work, in the words that carry it: CONFIG's to say, never the rules'. */
const MECHANICS = ['40 lines', '4 KB', '12 KB', '8 KB', 'PreCompact', 'SessionStart', 'PostToolUse', 'PostCompact', 'Stop'];

test('CONFIG carries the handoff hooks the rules file used to explain', () => {
  const md = read('docs/CONFIG.md');
  const hooks = section(md, '## The handoff hooks');
  for (const mechanism of MECHANICS) assert.ok(hooks.includes(mechanism), `CONFIG "The handoff hooks" carries: ${mechanism}`);
  for (const event of ['PreCompact', 'SessionStart', 'PostToolUse', 'Stop', 'PostCompact']) {
    assert.ok(hooks.includes(`\`${event}\``), `${event} named`);
  }
  for (const fact of [
    '<session cwd>/.omelette/ledger-*.md',
    'a ledger kept in another repository gets neither the stamp nor the print',
    '40 lines / 4 KB per ledger, 12 KB in all',
    '## Compaction summary <ISO> (trigger: …)',
    'Bounded to 8 KB',
    '`handoff.compactSummary=false`',
    '`handoff.enabled=false`',
    'remind **once** and gate **once** per crossing',
    'the handoff block is still yours to write',
    'A manual `/compact` below the threshold gets no reminder',
    'handoffs at every natural pause, and the hook is the net under it',
  ]) assert.ok(hooks.includes(fact), `CONFIG "The handoff hooks" says: ${fact}`);
  assert.match(md, /^\| .+ \| \[The handoff hooks\]\(#the-handoff-hooks\) \|$/m, 'the map at the top links it');
});
```

- [ ] **Step 1.2 — run it red.**

```bash
cd <repo root> && node --test test/rules-size.test.mjs
```

Expected: `# fail 1`, error `'## The handoff hooks present'`.

- [ ] **Step 1.3 — the map row.** In `docs/CONFIG.md`, old (line 11):

~~~md
| How do I configure the auto-handoff threshold and window? | [Handoff settings](#handoff-settings) |
~~~

new:

~~~md
| How do I configure the auto-handoff threshold and window? | [Handoff settings](#handoff-settings) |
| What do the handoff hooks do to my ledger, and when? | [The handoff hooks](#the-handoff-hooks) |
~~~

- [ ] **Step 1.4 — the link from Handoff settings.** Old (line 125):

~~~md
The `handoff` block configures the other managed file that is rendered rather than read at call time: the guard hook's auto-handoff, which asks for the `## Handoff` block once the session's context passes a threshold and holds one `Stop` until it is written.
~~~

new:

~~~md
The `handoff` block configures the other managed file that is rendered rather than read at call time: the guard hook's auto-handoff, which asks for the `## Handoff` block once the session's context passes a threshold and holds one `Stop` until it is written. What each hook does with these values: [The handoff hooks](#the-handoff-hooks).
~~~

- [ ] **Step 1.5 — the section.** Old (lines 170–172, the end of "Workflow settings" and the next heading):

~~~md
Validation is the same as everywhere else: an invalid value is a warning (`omelette-fleet rules` prints it on stderr and renders the default sentence), and `set` refuses it outright. There are no environment overrides for this block.

## Keys
~~~

new:

~~~md
Validation is the same as everywhere else: an invalid value is a warning (`omelette-fleet rules` prints it on stderr and renders the default sentence), and `set` refuses it outright. There are no environment overrides for this block.

## The handoff hooks

What `omelette-fleet rules --hooks` does for the ledger, event by event. The rules file keeps only the obligations; the mechanism is here. Every handler reads `<session cwd>/.omelette/ledger-*.md` — the directory the session runs in, not the repository the plan is about — so **a ledger kept in another repository gets neither the stamp nor the print**. None of it happens unless `.omelette/` holds a `ledger-*.md`: that file is the opt-in.

| Event | What the guard does | Bounds and switch |
|---|---|---|
| `PreCompact` | Appends `## Compaction <ISO> (trigger: …) — re-read this ledger before continuing` to every ledger, announced compaction or not, and drops this session's threshold crossing | Regular files only: a symlink or a FIFO named like a ledger is skipped. On whenever the guard is wired |
| `SessionStart`, matcher `compact` | Prints each ledger's **last handoff block** into the context that opens after the compaction, so the re-read is a paste rather than a search | Only for the source `compact`; 40 lines / 4 KB per ledger, 12 KB in all |
| `PostToolUse` | Reads the last request's token usage out of the session transcript; once the context passes `handoff.threshold` (90 % by default) of the window `handoff.contextWindow` resolves to ([Handoff settings](#handoff-settings)), puts one line into the context asking for a `## Handoff` block now | Once per crossing; silent in a sub-agent; `handoff.enabled=false` turns it off |
| `Stop` | Refuses the first `Stop` after the crossing while no `## Handoff` has been appended since; stop again and the turn ends | Once per crossing; silent in a sub-agent; `handoff.enabled=false` turns it off |
| `PostCompact` | Appends Claude Code's own summary of the compaction to every ledger under `## Compaction summary <ISO> (trigger: …)`: the account of what the context dropped, kept where the plan is kept | Bounded to 8 KB; `handoff.compactSummary=false` turns it off |

**The summary is not a handoff.** Its heading is deliberately not `## Handoff`, and a heading quoted inside it is escaped on the way in, so the `SessionStart` print and the `Stop` gate both ignore it: the handoff block is still yours to write.

**What the hooks do not do.** They remind **once** and gate **once** per crossing, and they print only the handoff block you wrote. A manual `/compact` below the threshold gets no reminder. The discipline is the ledger file, not the hook: handoffs at every natural pause, and the hook is the net under it.

A changed value reaches a session only through a re-render — `omelette-fleet set handoff.compactSummary=false && omelette-fleet rules --hooks` — because the values are substituted into the script ([Handoff settings](#handoff-settings)).

## Keys
~~~

- [ ] **Step 1.6 — run it green, with the docs-map and the CONFIG pins.**

```bash
cd <repo root> && node --test test/rules-size.test.mjs test/docs-map.test.mjs test/review-0.3.7-fixes.test.mjs test/tester-0.3.7-m5-m6-extra.test.mjs
```

Expected: `# fail 0`; `rules-size` reports 1 pass. Then `npm test`: `# fail 0`.

- [ ] **Step 1.7 — commit (the session, after acceptance).**

```bash
cd <repo root> && git add docs/CONFIG.md test/rules-size.test.mjs && git commit -F - <<'EOF'
docs: CONFIG gets a section on the handoff hooks — what each event does for the ledger, its bounds and its switch

The rules file is loaded into every session and explained five hook
events in prose; 1.2.0 moves that explanation out and keeps the rules.
The mechanism needs a home a reader can find from the config keys it
depends on, so it becomes one table next to the handoff.* keys, with
the obligations restated beside it. The new test pins every fact the
rules file carried, so the next commit moves text instead of losing it.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01854UFRDMB6pEz5bRWeYMhE
EOF
```

---

### Task 2: rules — "Ledger and handoff" keeps its five hook obligations, one line each, and points at CONFIG for how the hooks work

**Files:**
- Modify `rules/omelette-fleet.md`: lines 29–31 (three bullets out, six lines in). **No other line.**
- Modify `test/rules-size.test.mjs`: append three tests.

**Interfaces**
- *Consumes:* `rules`, `section`, `MECHANICS` (Task 1); `renderRulesFile(version, { merge })` from `core/rules.mjs` (unchanged); `docs/CONFIG.md` "The handoff hooks" (Task 1), which the new wiring sentence points at.
- *Produces:* the rendered rules file at 12 521 characters (`session`) / 12 489 (`pr`) under the 13 100 ceiling this task pins. Every heading, the routing table, the two evidence bullets, the small-change bullets and every other rule line are what they were in 1.1.0.

- [ ] **Step 2.1 — write the failing tests.** Append to `test/rules-size.test.mjs`:

```js

// ── the rules file: explanation out, every rule kept ─────────────────────────

/** Every heading the 1.1.0 file had. */
const HEADINGS = [
  '# Working with the omelette fleet',
  '## Operating model for the session',
  '## Ledger and handoff',
  '## Tester flow',
  '## Reviews',
  '## Spawning sub-agents: model and effort',
  '## Routing',
  '## Never a sole source',
  '## Briefing a unit',
];

/** The two bullets test/evidence-pointers.test.mjs pins, byte for byte as 1.1.0 shipped them. */
const EVIDENCE = '- **Evidence travels with pointers.** Coder and tester reports come as `TASK / FINDINGS / DIFF / TEST RESULTS / OPEN QUESTIONS`, each finding a pointer line — `path:line` (relative to the project root, one line) · a verbatim fragment in backticks · the claim. `omelette-fleet check <file>` verifies the pointers: check before you trust.';
const SCOUT = '- **One scout map per release, never to a first review.** Before planning several packages, one read-only scout writes `.omelette/map-<plan>.md` — `commit: <hash>`, factual pointer lines, a closing `## Not read` — and the planners get it instead of re-reading. Run `check` on it, open three pointers yourself, re-take stale lines.';

/**
 * The five obligations the three hook bullets carried, each by its full
 * sentence as the 1.1.0 text words it (a sentence that opened mid-bullet
 * gains its capital letter, nothing else), and each a bullet of its own.
 */
const DUTIES = [
  '**After a compaction, re-read the ledger before doing anything else.**',
  '**A ledger kept in another repository gets neither the stamp nor the print.**',
  'The handoff block is still yours to write.',
  'A manual `/compact` below the threshold gets no reminder.',
  'The discipline is unchanged: handoffs at every natural pause, and the hook is the net under it.',
];

test('the rendered rules file is at most 13 100 characters under either merge policy', () => {
  for (const merge of ['session', 'pr']) {
    const text = renderRulesFile('1.2.0', { merge });
    assert.ok(text.length <= 13100, `${merge}: ${text.length} characters`);
  }
});

test('the rules keep every heading and the two evidence bullets byte for byte', () => {
  const lines = rules().split('\n');
  for (const h of HEADINGS) assert.ok(lines.includes(h), `heading kept: ${h}`);
  assert.ok(lines.includes(EVIDENCE), 'the evidence bullet is unchanged');
  assert.ok(lines.includes(SCOUT), 'the scout-map bullet is unchanged');
});

test('"Ledger and handoff" states the five hook obligations in full, one line each, and points at CONFIG for the rest', () => {
  const text = rules();
  const lines = section(text, '## Ledger and handoff').split('\n');
  for (const duty of DUTIES) assert.ok(lines.includes(`- ${duty}`), `a line of its own: ${duty}`);
  const wiring = lines.filter((l) => l.includes('rules --hooks`'));
  assert.equal(wiring.length, 1, 'one sentence says what `rules --hooks` wires');
  assert.ok(wiring[0].includes('docs/CONFIG.md'), 'and points at CONFIG');
  for (const mechanism of MECHANICS) assert.ok(!text.includes(mechanism), `mechanism moved out of the rules: ${mechanism}`);
});
```

- [ ] **Step 2.2 — run them red.**

```bash
cd <repo root> && node --test test/rules-size.test.mjs
```

Expected: `# fail 2` — `session: 14390 characters`, and `a line of its own: **After a compaction, re-read the ledger before doing anything else.**` (today that sentence opens a longer bullet). The headings-and-evidence test passes already: it is there so the edit cannot take a heading or an evidence bullet with it.

- [ ] **Step 2.3 — replace the three hook bullets.** In `rules/omelette-fleet.md`, old (lines 29–31, three lines):

~~~md
- **After a compaction, re-read the ledger before doing anything else.** `omelette-fleet rules --hooks` installs both halves of that: a `PreCompact` hook that stamps the re-read line into every ledger, and a `SessionStart` hook matched on `compact` that prints each ledger's **last handoff block** back into the fresh context (bounded: 40 lines / 4 KB per ledger, 12 KB in all). Both read `<session cwd>/.omelette/ledger-*.md` — **a ledger kept in another repository gets neither the stamp nor the print.** The discipline is still the file, not the hook: what the hook prints is the handoff block you wrote.
- **The compaction summary lands in the ledger for the record.** `rules --hooks` also wires `PostCompact`: when a compaction finishes, the guard appends Claude Code's own summary of it to every `.omelette/ledger-*.md` under a `## Compaction summary <ISO> (trigger: …)` heading, bounded to 8 KB — the account of what the context dropped, kept where the plan is kept. It is not a handoff and the heading is deliberately not one (and a heading quoted inside the summary is escaped on the way in), so the `SessionStart` print and the `Stop` gate ignore it: the handoff block is still yours to write. Turn it off with `omelette-fleet set handoff.compactSummary=false && omelette-fleet rules --hooks`.
- **At the threshold the guard asks for the handoff, and holds one turn until it is written.** `rules --hooks` also wires `PostToolUse` and `Stop`: the guard reads the last request's token usage out of the session transcript, and once the context passes `handoff.threshold` (90 % by default, of `handoff.contextWindow` → `CLAUDE_CODE_AUTO_COMPACT_WINDOW` → `autoCompactWindow` in your user settings (`$CLAUDE_CONFIG_DIR` or `~/.claude`, the `.local` file first) → `model[1m]`, a model id ending in `[1m]`, which is 1 000 000 (`ANTHROPIC_MODEL`, then the `model` key of those same two files) → `default`, 200 000) it puts one line into the context asking for a `## Handoff` block now, then refuses the first `Stop` that follows while none has been appended. It reminds **once** and gates **once** per crossing — stop again and the turn ends — and it says nothing at all unless `.omelette/` holds a `ledger-*.md`, which is how a project opts in. A manual `/compact` below the threshold gets no reminder. The discipline is unchanged: handoffs at every natural pause, and the hook is the net under it.
~~~

new (six lines):

~~~md
- **After a compaction, re-read the ledger before doing anything else.**
- `omelette-fleet rules --hooks` wires the ledger hooks, which say nothing unless `.omelette/` holds a `ledger-*.md`; how each one works: `docs/CONFIG.md`, "The handoff hooks".
- **A ledger kept in another repository gets neither the stamp nor the print.**
- The handoff block is still yours to write.
- A manual `/compact` below the threshold gets no reminder.
- The discipline is unchanged: handoffs at every natural pause, and the hook is the net under it.
~~~

- [ ] **Step 2.4 — run it green, with every test that pins the rules text, and measure.**

```bash
cd <repo root> && node --test test/rules-size.test.mjs test/rules.test.mjs test/rules-tester.test.mjs test/evidence-pointers.test.mjs test/review-0.3.7-fixes.test.mjs test/tester-0.3.7-m5-m6-extra.test.mjs test/tester-0.3.4-p2-review.test.mjs test/docs-map.test.mjs
node --input-type=module -e "import { renderRulesFile } from './core/rules.mjs'; for (const merge of ['session', 'pr']) { const t = renderRulesFile('1.2.0', { merge }); console.log(merge, t.length, Buffer.byteLength(t), 13100 - t.length); }"
```

Expected: `# fail 0`, `rules-size` reports 4 pass; the measurement prints `session 12521 12592 579` and `pr 12489 12560 611`.

- [ ] **Step 2.5 — prove nothing else in the template moved.**

```bash
cd <repo root> && git diff --numstat -- rules/omelette-fleet.md && git diff -U0 -- rules/omelette-fleet.md | grep '^@@'
```

Expected: exactly `6	3	rules/omelette-fleet.md` and exactly one hunk header, `@@ -29,3 +29,6 @@`. Anything else — a second hunk, another count — means a line outside the three hook bullets changed: undo it before going on.

- [ ] **Step 2.6 — the full suite.**

```bash
cd <repo root> && npm test
```

Expected: `# fail 0`.

- [ ] **Step 2.7 — refresh this checkout's rendered copies (not a commit).** `.claude/` is gitignored; the template changed, so the copies this checkout's sessions load are stale:

```bash
cd <repo root> && node bin/omelette-fleet.mjs rules --agents --hooks && git status --short .claude
```

Expected: the run reports the rules file written (same version, content changed) and prints the settings snippet, which needs no pasting — the wiring is unchanged; `git status --short .claude` prints nothing (ignored). The new rules reach a session at its next start, not the running one.

- [ ] **Step 2.8 — commit (the session, after acceptance).**

```bash
cd <repo root> && git add rules/omelette-fleet.md test/rules-size.test.mjs && git commit -F - <<'EOF'
rules: the ledger section keeps its five hook obligations, one line each, and points at CONFIG for how the hooks work (14 390 → 12 521 characters, pinned at 13 100)

Three bullets explained PreCompact, SessionStart, PostCompact,
PostToolUse and Stop to every session on every request. What binds the
session in them is five sentences, and those stay word for word, each
on a line of its own: re-read after a compaction; another repository's
ledger gets neither stamp nor print; the handoff block is still yours
to write; a manual /compact below the threshold gets no reminder; the
discipline is unchanged. One sentence says what rules --hooks wires,
that the hooks say nothing without a .omelette/ledger-*.md, and where
CONFIG explains how each one works. No other line of the file changed;
the diff is one hunk.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01854UFRDMB6pEz5bRWeYMhE
EOF
```

---

### Task 3: MEASUREMENTS — the rules file, before and after

**Files:**
- Modify `docs/MEASUREMENTS.md`: after the map row "What does the short fleet contract save in every session?" (line 10); after the paragraph "In tokens that is roughly 900 down to 120 …" (line 71); after the bullet "- **Contract sizes.** …" (line 142) — numbering at `6951a5e`; each anchor occurs exactly once in the file.

**Interfaces**
- *Consumes:* the template at the end of Task 2; tag `v1.1.0` for the "before".
- *Produces:* heading `## The rules file, before and after`, anchor `#the-rules-file-before-and-after`. Docs only: no new test; `test/docs-map.test.mjs` is the gate (a new heading with its map row).

- [ ] **Step 3.1 — precondition.** P0 writes rows into the same file. This task commits only its own three insertions:

```bash
cd <repo root> && git diff --quiet HEAD -- docs/MEASUREMENTS.md && echo clean
```

Expected: `clean`. If not, another package's rows are uncommitted: stop and report to the session, which commits them as that package's own commit first. Never commit them in this task.

- [ ] **Step 3.2 — measure.**

```bash
cd <repo root> && node --input-type=module -e "
import { execFileSync } from 'node:child_process';
import { MERGE_SENTENCES, RULES_MARKER, renderRulesFile } from './core/rules.mjs';
const render = (raw, v) => { const t = raw.replaceAll('{{marker}}', RULES_MARKER(v)).replaceAll('{{version}}', v).replaceAll('{{merge}}', MERGE_SENTENCES.session); return t.endsWith('\n') ? t : t + '\n'; };
const before = render(execFileSync('git', ['show', 'v1.1.0:rules/omelette-fleet.md'], { encoding: 'utf8' }), '1.1.0');
const after = renderRulesFile('1.2.0', { merge: 'session' });
console.log('before', before.length, Buffer.byteLength(before));
console.log('after', after.length, Buffer.byteLength(after));
console.log('saved', before.length - after.length, Buffer.byteLength(before) - Buffer.byteLength(after));
"
```

Expected: `before 14390 14479`, `after 12521 12592`, `saved 1869 1887`. If the "after" differs (the template was changed in review), write the printed numbers into the table below and recompute the tokens as characters ÷ 4, rounded to the hundred.

- [ ] **Step 3.3 — the map row.** Old:

~~~md
| What does the short fleet contract save in every session? | [The fleet contract, full and short](#the-fleet-contract-full-and-short) |
~~~

new:

~~~md
| What does the short fleet contract save in every session? | [The fleet contract, full and short](#the-fleet-contract-full-and-short) |
| How big is the rules file every session loads, before and after 1.2.0? | [The rules file, before and after](#the-rules-file-before-and-after) |
~~~

- [ ] **Step 3.4 — the section.** Old:

~~~md
In tokens that is roughly 900 down to 120 at four characters per token — the ratio is exact, the token figures are an estimate.
~~~

new:

~~~md
In tokens that is roughly 900 down to 120 at four characters per token — the ratio is exact, the token figures are an estimate.

## The rules file, before and after

The rendered `.claude/rules/omelette-fleet.md` is loaded at every session start, so every character of it is resident in every session. 1.2.0 kept every rule in it word for word and moved out only the prose that explained how the handoff hooks work, to CONFIG ("The handoff hooks").

| Rendered rules file | Characters | UTF-8 bytes | Tokens, estimated |
|---|---:|---:|---:|
| 1.1.0 | 14 390 | 14 479 | ~3 600 |
| 1.2.0, after P1 | 12 521 | 12 592 | ~3 100 |
| **Saved, resident in every session** | 1 869 | 1 887 | **~500** |

Rendered under the default merge policy (`session`; the `pr` sentence is 32 characters shorter). Tokens at four characters per token, as for the contract above: the character counts are exact, the token figures an estimate. The ceiling is 13 100 characters, pinned by `test/rules-size.test.mjs`.
~~~

- [ ] **Step 3.5 — how it was measured.** Old:

~~~md
- **Contract sizes.** `FLEET_CONTRACT.length` and `SHORT_CONTRACT.length` from `core/rules.mjs`.
~~~

new:

~~~md
- **Contract sizes.** `FLEET_CONTRACT.length` and `SHORT_CONTRACT.length` from `core/rules.mjs`.
- **Rules file size.** `renderRulesFile('1.2.0', { merge: 'session' })` from `core/rules.mjs` — `.length` for characters, `Buffer.byteLength` for bytes — and, for 1.1.0, the same three substitutions applied to `git show v1.1.0:rules/omelette-fleet.md`.
~~~

- [ ] **Step 3.6 — run the gate.**

```bash
cd <repo root> && node --test test/docs-map.test.mjs && npm test
```

Expected: `# fail 0` for both.

- [ ] **Step 3.7 — commit (the session, after acceptance).**

```bash
cd <repo root> && git add docs/MEASUREMENTS.md && git commit -F - <<'EOF'
docs: MEASUREMENTS — the rules file before and after 1.2.0 (14 390 → 12 521 characters, ~500 tokens less in every session)

The spec owes this row: the rules file is the one text every session
carries on every request, and 1.2.0's claim that it got cheaper is a
number or it is nothing. Characters and bytes are exact; tokens are the
same four-characters estimate the contract row uses.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01854UFRDMB6pEz5bRWeYMhE
EOF
```

---

## Self-Review

This revision was not dry-run (the session's instruction; the reviewer and the build do that). Task 2's final text — with the ruled wiring line — was rendered once for the sizes (12 521 / 12 489), and every string the new tests assert was checked by script against that rendered text and against Task 1's CONFIG section: the nine `MECHANICS` strings are all absent from the rendered rules and all present in the section; the nine headings, the two evidence bullets and the five `- <obligation>` lines are all present as whole lines; exactly one line of "Ledger and handoff" holds `rules --hooks` and it names `docs/CONFIG.md`. A `diff` of the template before and after is one hunk, lines 29–31 → 29–34. Task 1 is unchanged from the dry-run-verified earlier revision except for the `MECHANICS` list, its presence check and the header comment.

**Spec coverage.**

| Spec requirement (P1, rewritten) | Where |
|---|---|
| The three hook-mechanics bullets → their five obligations, each its own sentence, verbatim | Task 2, Step 2.3; test: each `- <sentence>` is a whole line (Ruling 3, 5) |
| … plus one sentence naming what `rules --hooks` wires and pointing to CONFIG | Task 2, the wiring line, with the opt-in condition and the CONFIG section's name (Ruling 2); test: exactly one such line, naming `docs/CONFIG.md` |
| The mechanics prose → `## The handoff hooks` in CONFIG | Task 1 (section, map row, link from Handoff settings); test: every `MECHANICS` string present there |
| Small-change bullets and ORCHESTRATION's copy unchanged | Global Constraints; Step 2.5's one-hunk diff; ORCHESTRATION not in any task |
| Every other bullet, heading, table row and sentence byte for byte | Step 2.5 (`git diff`: `6	3`, one hunk `@@ -29,3 +29,6 @@`); Ruling 8 |
| Ceiling 13 100 rendered, both policies | Task 2 test; 12 521 / 12 489, leaving P2 579 / 611 |
| Test pins the five sentences in full, every `##` heading, the hook-mechanics strings absent from the rules and present in CONFIG | Task 2 tests; Task 1 test |
| Unchanged: the two evidence bullets; the short-contract mechanism | Task 2 test (`EVIDENCE`, `SCOUT` as whole lines); `core/rules.mjs` untouched |
| `test/docs-map.test.mjs` passes after the CONFIG addition | Task 1 (row in the same commit), Task 3 |
| MEASUREMENTS P1 row | Task 3 |
| Rendered copies refreshed | Step 2.7 (not a commit) |

**Placeholder scan.** No `TBD`, `TODO`, "similar to", "as above" or "etc." in any step. The only `…` characters are literal ones in the shipped CONFIG text (`(trigger: …)`) or elisions in prose that quote a line given in full in a block. `<repo root>` is the working-copy convention of earlier plans. Every edit has its exact old and new text; every test is full code; every command has its expected output.

**Type and name consistency.** `read(rel)`, `rules()`, `section(md, heading)` and `MECHANICS` are defined once (Task 1) and used by Task 2 with those names. `renderRulesFile('1.2.0', { merge })` matches `core/rules.mjs:256`. `## The handoff hooks` → `#the-handoff-hooks` and `## The rules file, before and after` → `#the-rules-file-before-and-after` under `test/docs-map.test.mjs`'s slug rule. The five `DUTIES` strings are byte-identical to the five obligation lines of Step 2.3 without their `- `.

**Atomicity.** Three commits, each one behaviour with its test: the destination section (1), the rules change with the pins that describe it (2), the measurement row (3). Task 1 passes on its own (it adds a section and a test of that section); Task 2's tests read Task 1's section and fail without Task 2's edit; Task 3 is docs, gated by `docs-map` and the full suite.
