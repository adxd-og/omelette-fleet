# Changelog

## 0.3.7 — 2026-09-11

- **The fleet contract costs nothing where the rules are already installed.** A unit server now looks, at startup, for the rendered `.claude/rules/omelette-fleet.md` in the directory it was started in — the session's project — and then for the global one; where it finds one carrying our marker, `initialize` returns `omelette-fleet: read-only unit; the operating model is in .claude/rules/omelette-fleet.md — the units propose, you apply.` plus the unit's own line, instead of the ~300-token fleet contract three servers were each putting into every session that already had the longer text loaded. A file without our marker is somebody else's and counts as absent; at most 8 KiB of it is read, only line 1 is looked at, and nothing from it is copied anywhere. The new top-level `contract` key (`auto | full | short`, default `auto`) decides it outright for an operator who would rather not be detected — `omelette-fleet set contract=short`, `omelette-fleet show fleet` — and `doctor` prints what this project gets under the `handoff` line: `contract      short (rules installed here)`, `full (no rules file in <cwd>)`, `full (contract=full)`. It is resolved when a server starts, so a change reaches the next session, exactly like the rules file it detects.
- **The output tail is a queue, not a string that is rebuilt per chunk.** `core/spawn.mjs` kept the last `outputCap` characters of a run's stdout by concatenating every chunk onto a string and slicing it, which flattened and re-copied the whole tail on every chunk: 50 MB of 64 KiB output under a 4 MB cap moved about 3 GB of string through the heap to keep 4 MB of it. Chunks now go into an array with a running total, the head is dropped while what remains still covers the cap, and the array is joined once when the process closes — the leading excess coming off the head chunk rather than off a second copy of the tail. Measured on that case: peak heap 68 MiB → 22 MiB, 2.8 s → 0.1 s. Behaviour is identical for every caller — the same `stdout`, the same `capped` (characters dropped or sliced, so output landing exactly on the cap is still not capped), the same untouched `stderr` — and every existing cap test passes unchanged.
- **The spooled record says what the call cost.** Every result now carries a `usage:` header line — `input=<n> output=<n> [cachedInput=<n>] [reasoning=<n>]`, directly under `effort:` — written from the same object the status feed's `end` event gets: all four counts from Codex, the pair from Gemini and Grok, nothing from an image run. The line is written only for a complete input/output pair, so a call whose vendor said nothing about tokens has no line rather than a row of zeros, and a half-reported run is not passed off as a measured one. `parseResult` answers `usage: { input, output, cachedInput?, reasoning? } | null`, skips a key it does not know, and a record written before 0.3.7 reads as "no usage" — as does the new line to a reader from before 0.3.7, which skipped unknown header keys already.
- **`omelette-fleet results --stats [<unit>] [--since <when>]` — what a plan cost.** One row per unit that has records, and a total: calls, `ok/error/cancelled`, partial, wall time as `1h 2m 3s`, the bytes those records occupy, and `tokens in / out`. The token cell is a number only when every call in the row reported one; otherwise it reads `n/a (<k> of <n> calls reported)`, because a total of an unknown fraction of the work is worse than no total. `--since` takes `24h`, `7d` or a date (`2026-09-09`, or a whole ISO timestamp), filters on `startedAt` and prints the window it used; a value that is neither — and `--since` without `--stats` — is exit 1 with one line. Everything else is exit 0: an empty spool, and a window with nothing in it, print `no results`. It reads the same files `results` lists, so it starts no server, spawns no vendor CLI and spends nothing. Retention still bounds it: what `resultsKeep` and `resultsMaxBytes` left on disk is what there is to count.
- **The compaction summary lands in the ledger.** `HOOK_EVENTS` gains a sixth event, `PostCompact` (no matcher; the snippet gets a sixth group and `doctor` reports an install still wired the 0.3.6 way as `NOT wired (missing PostCompact)`). When a compaction finishes, the guard reads the same 256 KiB transcript tail the auto-handoff measures against, takes the summary Claude Code just wrote — the last `isCompactSummary` record after the last `compact_boundary`, its `message.content` as a string or as the text parts of an array — and appends it to every `.omelette/ledger-*.md` under `## Compaction summary <ISO> (trigger: manual|auto)`, bounded to 8 KB at a line boundary with a `[… truncated]` line and any fence it left open closed. That heading is **not** a handoff heading on purpose, and every Markdown heading inside the body is written with a backslash in front of it, so a summary that quotes `## Handoff` cannot pass for one either: `SessionStart(compact)` still prints only the last `## Handoff` block, and the `Stop` gate still holds a turn until you write one — the summary is the record of what the context dropped, and the handoff is still yours. No summary record, a transcript it could not read, or `.omelette/` without a ledger: nothing written, nothing said, exit 0. `handoff.compactSummary` (boolean, default `true`) renders into the guard like the rest of the block — `omelette-fleet set handoff.compactSummary=false && omelette-fleet rules --hooks` — and `doctor`'s handoff line reads it back out of the installed script: `handoff       nudge at 90% of 200000 (default) · Stop gate on · summary on · ledgers: 1`.
- **The merge policy is config, and the rules say what you chose.** A fourth top-level block, `workflow`, with one key: `merge: session | pr`, `session` by default. `omelette-fleet rules` renders it into the one sentence of `.claude/rules/omelette-fleet.md` that says how a finished feature branch reaches main — `session` keeps *"The session merges the branch into main itself once every review is clean and it is confident the work is ready; pushing and tagging wait for the operator's explicit approval"*, `pr` writes *"The session opens a pull request from the feature branch and never merges into main itself; merging is the operator's or the repository's gate"*. `omelette-fleet set workflow.merge=pr && omelette-fleet rules` switches it, `show workflow` lists it with its source, and — like every other rendered value — a `set` alone changes nothing until the file is written again. `doctor` prints `merge policy  session` in its per-project block and `install --rules` prints `merge policy: session` after the files it wrote; while the policy is `session` and the repository carries a `.github/PULL_REQUEST_TEMPLATE.md`, a `CODEOWNERS` (at the root or under `.github/`) or — asked through `gh` only when it is on PATH and only when neither file said so, bounded at 5 s and silent on any failure — a protected `main`, the line ends with one hint: `— this repository looks PR-gated: consider set workflow.merge=pr`. A hint, never a fault: no exit code moves, and a `pr` policy is never questioned. The fleet still opens nothing and merges nothing; this changes what the rules TELL the session to do.
- **The small-change lane.** The rules file and ORCHESTRATION gain the other half of 0.3.6's small-change exception. A change that qualifies goes to main on its own short `fix/<name>` branch — written with its test, the suite green and green on Linux per the Linux rule, fast-forwarded, one ledger line — with no spec, no plan, no reviews and no tag; the next release's whole-branch review covers it, with the reviewers told which commits came through the lane, and its CHANGELOG line rides that release. What stays out is named beside it: anything that changes behaviour beyond the fix, touches the guard's refusal logic, the env allowlist, the billing scrub, the write gates or a tool's schema, or needs a doc section rewritten goes through the flow whatever its size.

## 0.3.6 — 2026-09-10

- **The research tools take a `cwd`, and the sandbox probe stops moving the process.** `gemini_research`, `grok_research` and `codex_research` gain an optional absolute `cwd` — validated exactly like the review tools' (`must be an absolute path`, `is not an existing directory`), refused before any spawn, passed as `--cwd` to grok and `-C` to codex, and used as the spawn directory for all three; omit it and nothing changes. `codex_research` stays `-s read-only` with one. The spooled record's `cwd:` header names it. That is what `doctor --probe-sandbox` needed: it asks the unit to run in its throwaway directory instead of standing in it, so `process.chdir` is gone from the CLI, and a relative `OMELETTE_HOME` behaves under the flag exactly as it does without it.
- **The probe's deadline ends the run.** The probe now passes an `AbortController` into its call and aborts it when the deadline fires, with the runtime built `cancel: kill` for that call alone (`createUnitRuntime(unit, { cancel })`, internal to doctor and documented as such — your configured policy is untouched for every real call). Before, doctor printed `skipped (timed out after N s)` and then sat there until the vendor's own bounds expired — up to `timeoutS + 60 s` for gemini — because an abandoned child's open pipes keep the process alive. Now the process group is SIGKILLed at once and `doctor` returns within the cap. The verdict rules are unchanged: the directory is read first, and a file already written is still `BREACHED`.
- **Codex's output cap fits a long agentic review.** The built-in `outputCap` goes from the fleet default 400 000 to **4 000 000** (`CODEX_OUTPUT_CAP`), used by the unit's `builtin` and by `extractResult`'s default. Codex's JSONL is one line per item — every reasoning step, every read-only command it ran in the sandbox, every file it read — and the answer is the LAST `agent_message`, so a cap that bites takes narration, and one that bites too hard loses the answer and the whole paid run with it. grok's 10 000 000 and gemini's 400 000 are unchanged, the key is still per-unit config, and `show codex` reports the new number as a `default`.
- **A cancelled Grok run keeps the error Grok had already reported.** When the stream carried a CLI-level failure and the client then cancelled the run, the marker and the error now say so: `[grok: cancelled by the client — Grok had reported: <error>]` on a salvaged answer, `grok cancelled by the client — Grok had reported: <error>` when there was nothing to salvage. A run that was already failing is not the same event as one that was merely still going, and the person who stopped it is the last one able to tell them apart. Which branch wins is unchanged — the outcome is still `cancelled` — and a hard kill that is not a cancel keeps its own wording.
- **Image tools answer with a path, or fail.** `gemini_image`, `grok_image`, `grok_image_edit` and `codex_image` return the artifact they stat-ed on disk and nothing else. A run that was capped, hard-killed or cancelled with the file already saved returns the bare path, with `partial: true` carrying the incompleteness into the status feed and the spooled record instead of a marker stapled to a string the caller is about to `stat`; `codex_image` returns an `image.png` it finds on disk even when the interpreter refused the run. With no artifact the tool fails, naming the bound that explains it — `raise <unit>.outputCap`, `raise <unit>.timeoutS`, or the client's own cancellation. `gemini_image` used to hand back agy's prose about the file; it hands back the file.
- **The rules say when, not only who.** `rules/omelette-fleet.md` and `docs/ORCHESTRATION.md` add the delegate-justification test — independent, a named uncertainty, a wrong answer that costs something, one-sentence output, all four or the session does it itself — and the 20-line small-change exception; a new `## Reviews` section makes the finding format mandatory (`location · scenario · consequence · how to confirm`, ruled `verified` / `rejected (reason)` / `needs-check (what)`) and states that the first review of a change runs clean while a re-review is briefed with the prior findings and rulings; and the ledger closes a plan with one `review yield: <findings> found / <accepted> accepted / <rejected> rejected — <the two or three that mattered>` line.

## 0.3.5 — 2026-09-09

- **A `[1m]` model id is a 1 000 000-token ceiling.** 0.3.4's auto-handoff measured a 1M session against Claude Code's documented 200 000 whenever `autoCompactWindow` was unset — it read a fresh context as 144 % full and asked for the handoff on the first tool call. Claude Code writes the model it is running into your settings, suffix and all, so the guard and `doctor` now read it: a value ending in `[1m]` (case aside, whitespace trimmed) in `ANTHROPIC_MODEL`, else in the `model` key of your user-scope `settings.local.json` / `settings.json`, is a 1 000 000 window. The chain becomes `handoff.contextWindow` → `CLAUDE_CODE_AUTO_COMPACT_WINDOW` → `autoCompactWindow` → `model[1m]` → 200 000, and `autoCompactWindow` still wins over the suffix: a window capped on purpose was meant. Nothing else about the id is read, no list of model names is kept, and the id is never printed — the hook reads one string and answers one number. A `[1m]` passed only as `claude --model …[1m]` is invisible to a hook and stays that way; that session wants `handoff.contextWindow`. The nudge and doctor's `handoff` line name the new source: `handoff       nudge at 90% of 1000000 (model[1m]) · Stop gate on · ledgers: 1`.
- **`doctor` names an unreadable settings file whichever reader tripped over it.** `readClientSetting` — the reader behind the ceiling on the `handoff` line — now returns `unreadable: [paths]` beside its value, by the same rule `readClientEnv` uses: a file that is absent is skipped in silence, one that exists and cannot be read or does not parse into an object is named, and the scan continues past it to the file that does parse. The three lists are merged, so a broken file still gets exactly one line — and the wording is now one sentence for every source: `settings: <path> unreadable — its values were not consulted` (was "env values in it"), because two of the three readers were never after `env` values.
- **Grok's output cap fits a long review.** The built-in `outputCap` goes from 2 000 000 to **10 000 000** characters. Grok's streaming NDJSON carries every thinking delta, every tool call and its arguments, and one JSON envelope per text delta, all in the stream the answer rides — and the 0.3.4 release review reached the old cap on a single 869-second `grok_code_review` whose stream carried every file Grok read. That answer survived only because the authoritative `result` line is the last thing printed; one line more and the call would have failed with `the final result line was lost`. The fleet default stays 400 000 and codex and gemini stay on it; the cap is still a per-unit config key (`omelette-fleet set grok.outputCap=…`); and `capped`/`partial` behaviour is untouched — a capped stream that still parses comes back marked, one whose `result` line was cut open is still a refusal naming `grok.outputCap`. The cost is memory, bounded by the cap itself, for a unit that runs one process at a time.
- **A deep-research result is filed under the models it actually used.** `gemini_deep_research` picks its stage models out of the catalog by tier and effort, which meant a call the operator configured no model for was spooled under `(vendor default)` while it had just chosen two. It now reports them through `ctx.usedModel` the moment `stageModels` resolves and **before the first stage spawns**: the `model:` header reads `<id> (decompose, gather) + <id> (synth)`, collapsed to the single id when an explicit or configured model made every stage the same. Because the report happens first, a run that is cancelled mid-pipeline or dies on the output cap is still filed under the models it asked for. `gemini_research` and `gemini_image` deliberately report nothing — agy picks their model and never says which — so `(vendor default)` stays their honest header, and the status feed is unchanged: it still records what the runtime resolved.
- **`doctor --probe-sandbox` proves the sandbox instead of describing it.** Opt-in, one real call per unit that is enabled, registered and has a binary: the unit is asked, through its research tool, to write `probe.txt` into a fresh private directory under the OS temp dir, and the verdict is read off the filesystem, never off the reply — `held`, `BREACHED` (the only sandbox condition that makes doctor exit 1) or `skipped`, with its reason: `disabled`, `not registered`, `registered elsewhere`, `binary not found`, `temp dir: …`, `timed out after N s`, `call failed: …` or `could not inspect: …` — a run that never answered, or answered with its own failure, proves nothing either way. A write gate left open for that unit is named on the line, because a `BREACHED` under an open gate is the gate working. The directory is removed in every path.

## 0.3.4 — 2026-09-09

- **The handoff writes itself before the context is gone.** 0.3.3 closed the loop *after* a compaction — `PreCompact` stamps the ledger, `SessionStart(compact)` prints the last `## Handoff` block back — and could not make sure that block existed. The discipline fails exactly when it matters: a long turn fills the window and auto-compaction fires before anyone thought of the ledger. So the guard now measures. It reads the tail of the session transcript (the last 256 KiB, regular file only) for the newest `message.usage` record, adds `input_tokens + cache_read_input_tokens + cache_creation_input_tokens` — the prompt that was just sent — and divides by the context window: the rendered `handoff.contextWindow` when it is not 0, else `CLAUDE_CODE_AUTO_COMPACT_WINDOW`, else `autoCompactWindow` in your user settings (`$CLAUDE_CONFIG_DIR` or `~/.claude`, `.local` first; both accept `500k` / `1m`), else Claude Code's 200 000. Past `handoff.threshold` — 90 % by default — one `PostToolUse` hook output puts ``omelette-fleet: context at 91% of 200000 tokens (default). Append a `## Handoff` block to .omelette/ledger-<plan>.md now — where the work stands, open findings, agents in flight, next action — auto-compaction is close.`` into the session's context, and the first `Stop` after that is refused with `decision: "block"` while no `^## Handoff` line has been appended to a ledger since the crossing. It reminds **once** and gates **once** per crossing: a session that stops again stops. The crossing lives in `<cwd>/.omelette/handoff-state.json` (0600, one entry per `session_id` with the ledger sizes at that moment, pruned at 7 days, capped at 64 entries); `PreCompact` clears it, and so does a fill that falls back below the threshold. Nothing happens at all without a `.omelette/ledger-*.md` — the ledger is the opt-in — nor inside a sub-agent, nor when anything was unreadable: no measurement is silence, and the guard still never fails a turn.
- **`handoff` is a config block, and `doctor` reports the value that is actually running.** `handoff.enabled` (boolean, `true`), `handoff.threshold` (50–99, `90`) and `handoff.contextWindow` (≥ 0, `0` = resolve at run time) live at the top level of `fleet.config.json` beside `agents`, are edited with `omelette-fleet set handoff.threshold=85` and listed by `omelette-fleet show handoff`. The guard imports nothing from the package, so the three values are substituted into the script at `rules --hooks` time — which means a `set` alone changes nothing, and `doctor` reads the numbers back **out of the installed script**: `handoff       nudge at 90% of 200000 (default) · Stop gate on · ledgers: 1`, with the ceiling's source named and `ledgers: none (hook silent — start .omelette/ledger-<plan>.md)` where there is nothing to guard.
- **`rules --hooks` prints five groups.** `HOOK_EVENTS` is `PreToolUse`, `PreCompact`, `SessionStart`, `PostToolUse`, `Stop`; the two new groups carry no matcher, because every tool call grows the context and every `Stop` is one. An installation still wired the 0.3.3 way reads `NOT wired (missing PostToolUse, Stop) — paste the snippet from rules --hooks`.
- **The tester is contained like the coder, and the refusal names the role it caught.** The guard's `PreToolUse` set is both shipped roles — `omelette-coder` and `omelette-tester` — where 0.3.3 enforced the coder's half and left the tester's to its prompt: a tester that stashed the tree to get a clean run was hiding the very diff the orchestrator was about to review, and nothing stopped it. Each refusal now says whose it is — `omelette-tester never commits, merges, rebases, pushes, stashes, tags, branches or opens worktrees; report instead` — because two roles reading one another's line is how an agent decides the block was meant for somebody else. A name is matched whole: `omelette-coder-2` is somebody else's agent, and the main thread carries no `agent_type` at all. Neither role loses a read.
- **A rebase option's value is a value.** `git rebase --exec --abort main` is not an abort: git replays the commit, tries to run `--abort` as the exec command and leaves the repository in `.git/rebase-merge` — and the guard passed it, because it saw the word `--abort` sitting among the arguments. The rebase classifier now steps over an option's value the way the tag one does, and the options that take the next word are `--exec`/`-x`, `--onto`, `--strategy`/`-s`, `--strategy-option`/`-X`, `--whitespace`, `--empty` and `-C` — the last three measured against git 2.38.1, which answers `git rebase --whitespace --abort main` with `fatal: Invalid whitespace option: '--abort'`, `--empty` with `fatal: unrecognized empty type '--abort'` and `-C` with ``fatal: switch `C' expects a numerical value``, so in none of them is the word an abort. The attached `--exec=--abort` form was never the bare word, a quoted option is still an option (`"--exec"` takes its value; quoting hides an ACTION word and nothing else), and an abbreviation of one takes the same value git gives it (`--ex --abort main` is refused). `--gpg-sign`/`-S` is **out** of that set, where 0.3.3 put it: its key-id is only ever attached, so `git rebase -S -h` prints usage and writes nothing — and now passes again. `git rebase --abort`, `--quit`, `--help`/`-h` and `git rebase -q --abort` are unchanged.
- **A spooled result is never filed under no model.** The tool context gains `ctx.usedModel(id)`: an adapter that pins a model the runtime did not choose says so, and `finish()` writes the `model:` header as the explicit or configured id, else that report, else the literal `(vendor default)`. Codex pins `catalog.ids[0]` on every run because `--ignore-user-config` means the operator's own `~/.codex/config.toml` default never applies, so its results used to be filed under an empty line; grok and gemini hand the choice to their CLI and never learn the id, and now say that in words instead of saying nothing. The report is idempotent (the first non-empty string wins, so a retried run cannot file two answers about one call), takes strings only, is never checked against the catalog — what the CLI was told is the fact worth keeping — and a `local` tool gets a no-op, because a tool that never spawns produces no record. `ctx.model` and the status feed are untouched: the feed is still schema 1 and its `start` line still records what the runtime resolved. `omelette-fleet results <unit> <id>` and `<unit>_result` show the same string, both rendering the file they read.
- **`doctor` names a settings file it could not parse.** `readClientEnv` returns `unreadable: [paths]` beside its value: a settings file that is absent is skipped in silence as before, but one that exists and does not parse into an object is named — `settings: <path> unreadable — env values in it were not consulted` — once per file, merged with the paths the hook-wiring report already collects, so an operator with one broken file gets one line rather than one per reader. It is informational and never a fault, like every other line in that block, and nothing in this package writes a settings file.
- **The handoff line says when it read the global guard.** `handoffReport` takes the project's guard first and falls through to the global one, which a project carrying a 0.3.3 guard does silently; the line now ends `· read from the global guard` when it did, because the values it shows are then changed with `rules --global --hooks` rather than with a re-render here.
- **CONFIG says why the idle wall is quieter than it looks.** Claude Code ≥ 2.1.263 passes `_meta.progressToken` on every `tools/call` and each progress notification resets the idle watchdog (verified in the shipped binary, 2026-09-08), so `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` only bites on a client that does not.

## 0.3.3 — 2026-09-08

- **Units report progress while a run is in flight.** A `tools/call` now reaches the runtime with the request's own context — its id, the client's `progressToken`, an abort signal and a way to send notifications — and while a child runs the unit sends `notifications/progress` every 30 s with the elapsed seconds and one short line. Nothing is sent to a client that supplied no token, and nothing after the response. The reason is a wall nobody had noticed: since Claude Code 2.1.203 a stdio tool call that sends neither a response nor a progress notification for 30 minutes is aborted for idleness, whatever `MCP_TOOL_TIMEOUT` says, and no unit sent either. `OMELETTE_DEBUG_META=1` logs the `_meta` of every `tools/call` to stderr — the one-line spike that settled whether a given client sends a token at all, kept as a debugging aid.
- **Cancellation has a policy: `cancel: "finish" | "kill"`, default `finish`.** `notifications/cancelled` used to be ignored, so a request the client abandoned kept running and its answer went nowhere. The transport now keeps an in-flight table keyed by request id (a number id and a string id are different requests), aborts the one it names, and drops that request's response — the MCP spec says a receiver ignores it, and sending it confuses a client that already moved on. `finish` leaves the vendor CLI alone, records the result, and marks the feed entry `detached: true`; `kill` SIGKILLs every process group the request owns, aborts a pending retry delay and stops a deep-research pipeline between stages, ending the call as `cancelled`. `finish` is the default because the units run on subscriptions with the billing keys scrubbed: a run that finishes after a cancel costs window quota, not money, and the incident behind this release was a lost answer.
- **Goodbye no longer kills a paid run.** A server used to exit the instant its stdin closed. It now stops reading, waits for the calls still in flight — each already bounded by that unit's `timeoutS` — and only then exits, so `finish` survives a client timeout and a client restart.
- **Every spawn-tool call carries a result id.** `<YYYYMMDDTHHMMSSZ>-<pid>-<seq>`, generated by the runtime and independent of the status feed (which plenty of installs switch off). It rides the feed's `start` line, `active` entry, `end` line and `lastEvent`, all still schema 1, alongside the new `detached` flag and the new `cancelled` status.
- **A `local` tool kind.** A tool answered in-process, with no spawn, no status-feed entry and a reduced context — what `<unit>_models` always was internally, now a first-class kind other tools can use.
- **The handoff block comes back after a compaction.** `PreCompact` stamped the re-read line into every `.omelette/ledger-*.md` and printed one `HANDOFF:` reminder — and that reminder went nowhere a contract promises: Claude Code's hooks reference documents `SessionStart` stdout as added to the session's context, and says nothing of the kind about `PreCompact`. So the guard gains a third event. On `SessionStart` with `source: "compact"` — and only that source; `startup`, `resume`, `clear` and `fork` lost no context — it prints each ledger's **last handoff block** to stdout, preceded by `--- <ledger> · last handoff ---`. A block is the last `## Handoff` heading to the next `## ` heading or the end of the file, with headings inside fenced code read as the text they are, so a ledger quoting its own vocabulary is not cut in half by its own example. It is bounded twice over — 40 lines and 4 KB per ledger, 12 KB across all of them, with a `[… truncated]` line where something was dropped and a ledger past the total cap dropped whole rather than half-printed — and each file is read at most 1 MiB and only when `lstat` says it is a regular file, so a symlink planted under that name is skipped rather than followed and a FIFO never blocks the read. Nothing to print is printed as nothing: no header, no blank line, exit 0. The guard still never fails a hook.
- **`rules --hooks` prints three groups, and `doctor` reads them.** `HOOK_EVENTS` is `PreToolUse`, `PreCompact`, `SessionStart`; the snippet's third group carries `"matcher": "compact"`. `doctor` reports `hooks project: v0.3.3 (wired: PreToolUse, PreCompact, SessionStart)`, and an installation still wired the 0.3.2 way is `NOT wired (missing SessionStart) — paste the snippet from rules --hooks` — the missing event by name, because that is what has to be pasted; an unreadable settings file or a matcher aimed elsewhere is the more specific reason and still wins the line. A `SessionStart` matcher is read exactly the way a `PreToolUse` one is, against the session's SOURCE instead of a tool name: `compact` and `startup|compact` are wired, an unanchored regex is tested as one, `startup` gets `NOT wired (SessionStart matcher is not compact)`, and an absent matcher counts as wired because the guard's own `source` check is what keeps a startup quiet.
- **The ledger has to live under the session's cwd.** Both halves of the mechanism read `<session cwd>/.omelette/ledger-*.md`, so a plan whose ledger sits in another repository gets neither the stamp nor the print. Said in the rules template, in `docs/ORCHESTRATION.md` and in the README.
- **`doctor` names the client's two timeout walls.** A `mcp timeout` block, informational and never a fault: the wall-clock `MCP_TOOL_TIMEOUT` compared with the longest call the enabled units can make (the largest `timeoutS`, plus 60 s for gemini, whose hard kill sits that far above the timeout it hands agy), and the 30-minute stdio idle abort a call hits when it sends neither a response nor progress. Unset is the client's documented ~28 h default and reads as `ok`; below what the fleet needs the line ends `· TOO LOW`, the snippet to merge follows (`{"env":{"MCP_TOOL_TIMEOUT":"<needed>"}}`), and a `next` line appears once every first-run step is done. Both variables are resolved the way Claude Code resolves them — process environment, then the `env` block of `.claude/settings.local.json`, `.claude/settings.json` and the user scope's files under `$CLAUDE_CONFIG_DIR` or `~/.claude` — and the line names the file the value came from. A per-server `"timeout"` on the registration overrides the variable for that server and gets a line of its own; one the client would refuse is reported as ignored rather than counted. While gemini is enabled, one more line bounds `gemini_deep_research` at `3 stages × 2 attempts × (timeoutS + 60 s)`. This is the wall the 2026-09-06 incident hit: a `codex_code_review` that ran 1081 s against a client that had abandoned the request at 900 s.
- **`doctor` finds this checkout's servers under any prefix, and in two more files.** A registration is ours by where it points — command node, args path exactly this clone's `servers/<unit>.mjs` — rather than by the name it wears, and the scan now covers the user scope, **this** project's scope inside `.claude.json` (another project's entries are not this project's) and the project's checked-in **`.mcp.json`**, which doctor did not read at all before. Without `--prefix`, when nothing of ours is registered as `omelette-*` and our servers are found under exactly one other prefix, doctor adopts it — `prefix        orion (found on the registrations)` — and the unit lines and the `next` line use it, so an operator running under their own prefix stops being told to install what is already installed. Two prefixes are ambiguous: doctor names both, keeps `omelette`, and leaves the choice to `--prefix`. An explicit `--prefix` is unchanged, and so are `install`, `uninstall` and `update`, which still work by name.
- **The guard's wiring is the union of a scope's settings files.** `settings.json` and `settings.local.json` are read as one: a guard wired for one event in the shared file and the rest in the machine-local one is wired, and doctor said `NOT wired` about it because it kept whichever single file listed the most events. A matcher that covers no `Bash` call is still reported — unless another file wires that event properly, in which case there is nothing to fix and nothing is claimed.
- **Codex and Gemini read `capped`, so a front-truncated run can no longer pass for a whole answer.** `core/spawn.mjs` has reported since 0.3.2 that its tail cap dropped characters — and a tail cap drops the *beginning*, so a parser reading from the top sees a fragment. Grok acted on it; the other two did not. Now they do, in the shape their CLIs need. **Codex**: a capped run whose last `agent_message` survived comes back with `[codex: output capped at <N> chars — the beginning of the stream was dropped; treat the answer as partial]` and `partial: true`, and a capped run whose answer rode a line longer than the cap throws `codex output exceeded the <N> char cap and the final message was lost — raise codex.outputCap or narrow the task` — where it used to report "produced no answer", naming a cause that was not the cause. **Gemini**: agy prints one JSON envelope, so a capped run that still parses (its dropped front was a preamble) is marked the same way, and one whose envelope no longer parses throws `agy output exceeded the <N> char cap and the answer envelope was lost — raise gemini.outputCap or narrow the task` instead of failing open with the middle of that envelope as the answer. In both units a hard kill is answered first — the salvaged text under both markers — and only a killed run with nothing to salvage names the cap, in an error naming both bounds; and both cap errors join the deterministic failures the bounded retry skips, because a second full run would be paid for and would hit the same cap. Neither unit raises its `outputCap`: both stay at the fleet default of 400 000.
- **A run the client cancelled says so instead of blaming `timeoutS`.** `cancel: kill` ends a run with the same SIGKILL a hard kill uses, and all three adapters read it as one: the salvaged answer carried `[<unit>: hard-killed after <N>s — … raise <unit>.timeoutS …]` and a run with nothing to salvage threw the matching error, sending the operator after a bound that had held. `core/spawn.mjs` tells the two apart (`cancelled: true`), so a cancelled run is now marked `[<unit>: cancelled by the client — treat the answer as partial]`, or refused with `<bin> cancelled by the client` when there was nothing to keep — deterministic either way, because nobody is waiting for the retry. All three spawning units read the flag: `agy cancelled by the client`, `grok cancelled by the client`, `codex cancelled by the client`.
- **`gemini_deep_research` says when its report stands on partial stages.** Every stage is one agy one-shot, so every stage can come back partial; the report used to present the synthesis of a salvaged kill as a finished document. It now carries `[gemini: N of M stages returned partial answers]` under the title — under the degraded-run banner when there is one — and `partial: true` beside `status: "ok"` in the status feed. M counts every stage that ran (decompose, one per sub-question, synthesis); a gather that *threw* is counted there and never in N, because its `_(gather failed: …)_` line already stands where its finding would be. A cancelled run, which never reaches synthesis, is partial by definition.
- **Docs.** `docs/CONFIG.md`'s "What `outputCap` does" now describes all three units instead of Grok plus a line saying the other two keep the default; `docs/STATUS-FEED.md` lists the third reason a run is `partial`; `docs/ADAPTERS.md` replaces the "Gemini and Codex do not read `capped` — it is a gap" paragraph with the three CLI shapes worth copying, and states the bare-path contract an image tool keeps (`codex_image` returns a verified path, never a path with a marker attached).

- **No paid answer is lost: every result is spooled before the response is sent.** `core/results.mjs` writes each spawned call's answer to `<home>/results/<unit>/<resultId>.md` — a `0700` directory, a `0600` file, a fenced header (unit, tool, result id, model, effort, `startedAt`/`endedAt`, `durationMs`, `status`, `partial`, `detached`, `cwd`, a 200-character JSON-quoted prompt preview) and then the answer verbatim — inside `finish()`, before anything reaches the client. It is fail-soft the way the status feed is: synchronous, every fs call wrapped, a failure is one stderr line and the call still answers. Errors and refusals are spooled too, a disabled unit's included: a call refused before the spawn is still an answer the caller may have lost. Local tools (`<unit>_models`, `<unit>_result`) never are. The write is an `O_EXCL` temp file in the same directory plus a `rename`, so a reader sees the whole file or none of it, and a temp file this process did not create is never removed — it belongs to another server's live write. Retention is per unit, after every write and at server start: `resultsKeep` (default 50) results inside `resultsMaxBytes` (default 50 MB), oldest by `endedAt` first with the filename as the tie-breaker, abandoned `*.tmp` older than an hour swept, every unlink guarded because two servers of the same unit share the directory. One answer bigger than the whole budget is kept anyway — it is the one that was just paid for. `results: false` switches writing off per unit or in `defaults`, and `doctor` says so on the unit's new `results` line.
- **`<unit>_result` — the answer the client dropped, handed back.** A new `local` tool on every unit (answered in-process: no spawn, no status feed entry, no spool of its own). With no `id`: the newest result in full, then `Recent results:` and up to ten lines of `<resultId>  <tool>  <status>[ partial][ detached]  <durationMs>ms  <startedAt>` — which is what a caller who timed out actually needs, since it never received the id. With an `id`: that result. An id must match `^\d{8}T\d{6}Z-\d+-\d+$` and is checked before any path is built, the file is `lstat`ed and read only when it is a regular file (a symlink in the spool is refused, never followed), and an unknown id or an empty spool is one line with `isError`. Reading is unaffected by `results: false`: the switch stops new writes, it does not hide what is already there.
- **`omelette-fleet results [<unit>] [<id>] [--path]`.** The same files from a shell, with no server and no vendor CLI: no arguments lists the last ten across the fleet newest first (`<resultId>  <unit>  <tool>  <status>[ partial][ detached]  <durationMs>ms  <startedAt>`), a unit lists its own, a unit and an id prints that file, and `--path` prints paths instead of content. An empty spool is exit 0 — nothing is broken; an unknown unit, a malformed id and an id that is not there are exit 1 with one line each. Reading never creates a directory.

## 0.3.2 — 2026-09-06

- **Ledger vocabulary.** The rules file and ORCHESTRATION's "Ledger and handoff" now ask for two things a plan ledger did not have: provenance apart from trust (a claim from a unit, coder or tester is `candidate` until the orchestrator verified it, then `verified` / `rejected`) and demote-not-delete (a wrong `Ruling:` is never edited — a `Superseded by:` line follows it). Prose only; no code, no tokens per turn.
- **Grok no longer answers from a front-truncated stream.** `core/spawn.mjs` keeps the LAST `outputCap` characters of a run's stdout, and it now reports `capped: true` when that cap actually dropped something — a tail cap drops the *beginning*, so a parser reading a stream from the top is looking at a fragment and could not tell. Grok is the unit where that bites: its streaming NDJSON carries thinking deltas, tool calls and one JSON envelope per text delta alongside the answer, and the authoritative `result` line comes last. `interpretGrok` now splits the two cases instead of failing open on both. Whole lines still parse: the answer is real but missing its start, so it comes back with `[grok: output capped at <N> chars — the beginning of the stream was dropped; treat the answer as partial]` appended and `partial: true` beside `status: "ok"` in the status feed, alongside the hard-kill marker when a run managed both. Nothing parses — the answer rode one `result` line longer than the cap — and the streaming runs throw `grok output exceeded the <N> char cap and the final result line was lost — raise grok.outputCap or narrow the task`, where 0.3.1 would have handed back the JSON fragment that survived as if it were the answer; the plain-stdout image runs have no lines to parse and keep their front-truncated text under the marker. A hard kill still answers first — salvaged text with both markers — and only a killed run with nothing captured names the cap, in an error that names both bounds. That failure joins the deterministic ones the bounded retry skips — a second full run would be paid for and would hit the same cap. A run that was killed AND left only a fragment is the same loss and now says so too, instead of salvaging the fragment under the partial marker; and a kill on a run the CLI had already reported an error for names the error (`grok hard-killed after 300s; the CLI had reported: auth expired`, and the same clause inside the capped-kill message), because an expired login reads nothing like a slow review and no bound explains one. **`outputCap` is a config key** (positive int, default 400 000, per unit or in `defaults`, threaded through `spawnFor` and overridable per call); Grok's built-in raises it to 2 000 000 for the reason above. Documented in `docs/CONFIG.md` (a section of its own), `docs/STATUS-FEED.md` (the second reason a run is `partial`) and `docs/ORCHESTRATION.md`.
- **`omelette-fleet install --rules` is the whole first run in one command.** It registers the units exactly as before, then does `rules --agents --hooks` in the current directory — the operating rules, both sub-agent definitions, the `/omelette-test` skill, the guard script and the settings snippet, printed last, where the eye already is. It runs on every exit path, `claude` missing included, because the project files have nothing to do with the registrations; `--dry-run` covers both halves and still writes nothing. A fleet home the CLI may not write is reported as the failure it is — `config  FAILED to write …` and exit 1 — and the rules phase still runs, since an operator who asked for both halves should not lose the one that had nothing to do with it. Both halves write into the CURRENT directory, so the README's Quickstart now says it in two steps: `install` from the clone, then the project files from inside the project.
- **`doctor` says what to do next.** One `next` line while something is missing, in the order a first run needs it: `omelette-fleet install` (nothing registered), `omelette-fleet rules --agents --hooks` (registered here, but any of the four managed kinds is missing from the project), `merge the hooks snippet into .claude/settings.json (rules --hooks prints it)` (the script is there and nothing calls it) — and nothing at all once those are done. A registration is "ours" only when it points at this clone's server, so a name pointing at another checkout still gets `install`; the whole check reads the project scope, which is what the commands it suggests write. The install line carries the prefix the run was asked about (`omelette-fleet install --prefix review`), because plain `install` would register `omelette-*` and leave the names doctor went looking for exactly as missing as it found them. A managed file that is at the right path without our marker gets a line of its own instead of the plain suggestion — `rules --agents --hooks` would refuse that file, and sending an operator into a refusal is not a next step: doctor names the situation and the `--force` that ends it. A managed path that is a SYMLINK gets a third line, and deliberately no `--force`: `rules` refuses to write through a link whatever flags it is given, so the step is to remove the link. It is informational and **never a fault**: the exit code still answers one question only, whether a unit that is enabled AND registered is broken.
- **A `PreToolUse` matcher is read the way Claude Code reads one: an exact list, or an unanchored regex.** Comparing the string literally reported a guard wired under `"Bash|Edit"` — which fires on every Bash call — as `NOT wired`; anchoring every pattern instead got the other half wrong, since the harness tests a pattern unanchored. Now: absent, `""` and `"*"` mean everything; a string of nothing but tool names, `|`, `,` and spaces is an exact LIST, compared item by item, so `"Bash|Edit"` and `"Bash, Write"` are wired while `"Bashful"` and `"ash"` name no tool at all; anything else is a regex tested against `Bash` unanchored, which wires `".*"`, `"Ba."`, `"^Ba"` and `"ash$"`. One that does not compile covers nothing rather than being guessed at, and gets a line of its own — `NOT wired (PreToolUse matcher "(" is not a valid regex)` — because a typo in the settings file and a guard aimed at another tool are fixed differently, and a matcher that is not a string at all gets a third — `NOT wired (PreToolUse matcher is not a string)`, an explicit `null` included, since a key that is there holding a non-pattern is not the same state as a key that was never written.
- **The guard tells reads from writes for tags and rebases, by the MODE of the invocation rather than the position of a word.** Listing tags is how an agent finds the last release and `git rebase --abort` is the recovery an agent stuck mid-rebase needs — both were blocked and both now pass, and so does every other spelling of the same intent, because both subcommands are now classified by reading their arguments: a `tag` invocation is a write when a write flag appears anywhere in it (`-a`/`--annotate`, `-m`/`--message`, `-F`/`--file`, `--cleanup`, `-s`/`-u`/`--sign`, `-f`/`--force`, `-d`/`--delete`), or when a tag NAME is present with no listing or verify flag beside it — so `-l`, `--list`, `-n`/`-n5`, `--contains`/`--no-contains`, `--with`/`--without`, `--points-at`, `--merged`/`--no-merged` and `-v`/`--verify` read whatever else is in the command, and `git tag v1 --list` lists where `git tag v1`, `git tag -- v1`, `git tag --create-reflog v1`, `git tag --no-sign v1` and `git tag v1 --format=x` all create — `--sort` and `--format` decorate a listing without selecting one, which is exactly why they are not in that set. `rebase` is the mirror image: `--abort`, `--quit` and `--help`/`-h` pass wherever among the arguments they sit (`git rebase -q --abort` included), and every other form is blocked, `--continue` and `--skip` among them, since each creates a commit. Both scans read the command the way a shell would. Comments come off first, each to the end of its OWN line (`git tag v1 # --list` creates a tag; the `git commit` in `echo x # git commit` is text; the line below `echo hi # a note` is a command of its own). The command is then tokenized once with quoted spans opaque, so a flag inside quotes is text (`git rebase --exec 'echo --abort now' HEAD~1` rebases), a separator inside quotes ends nothing (`git tag --format="x; y" v1` is one command that names `v1`), and an option's value is a value rather than a name whether it arrives attached or as the next word (`git tag --sort refname` and `git tag --format "%(refname)"` list; `git tag -m msg v1` still creates). Every step is a single linear pass — comments, tokens, flags — so the guard's hot path, every Bash call in the session, stays free of the option-run tiling the regexes were built to avoid: a listing narrowed 60 times over, or a 10 KB command, is answered in milliseconds.
- **`--remove --agents` takes the empty skill directory with it.** A skill IS a directory, so removing `SKILL.md` alone left `.claude/skills/omelette-test/` behind, still reading as an installed skill. `syncManagedFile` removes files and nothing else, as before; the skills kind gets one timid post-remove step — only that directory, only after a run that actually removed our `SKILL.md`, only when it is empty, never through a symlink, and a failure is silent rather than a refusal on top of a removal that worked.
- **The hook snippet is quoted for the platform the CLI runs on.** POSIX single quotes as before; where `process.platform` is `win32`, double quotes instead (`"node \"C:\\Users\\me\\.claude\\hooks\\omelette-guard.mjs\""`), because `cmd.exe` reads a single quote as part of the path. That is a correctness fix in the quoting, **not** Windows support — see the platform note below, and note that a double-quoted command is still shell-quoted: under PowerShell or Git Bash a `$` in the path would expand. The builder moved into `core/rules.mjs` beside the hooks kind it belongs to and takes the platform as a parameter, so both forms are tested from either kind of machine; `doctor` still recognises the wiring by the script's name, whichever quoting it finds.
- **`/omelette-test <spec path> [repo path]`.** The skill takes an optional second argument for an orchestrator whose session sits in another repository: `$0` is the spec, `$1` the repo, and the injected commands are `` !`git -C "$1" diff HEAD` `` and `` !`git -C "$1" ls-files --others --exclude-standard` ``. Leave it off and the shell expands `"$1"` to an empty string, where `git -C ""` is a no-op — the diff is the current directory's, exactly as in 0.3.1 (verified on Claude Code 2.1.261, 2026-09-06).
- **`maxTurns` in a definition is honoured — measured, not assumed.** An agent given `maxTurns: 3` and six tool calls to make stopped after three, and the harness told the orchestrator it had stopped at its turn limit and could be continued (2026-09-06). So the tester template now says to report what it had left to do, and the rules file and `docs/ORCHESTRATION.md` say to raise `agents.tester.maxTurns`, re-render, and **continue the same tester** rather than dispatching a new one, which throws its context away.
- **"Picked up within seconds" is now "usually within seconds, sometimes minutes".** A new global definition once took about ten minutes to appear (2026-09-06). The CLI's post-write line, the README and `docs/ORCHESTRATION.md` all say so; the watcher is real, its latency is not a guarantee.
- **A positive-int config key is a WHOLE number.** `coerce` floored what it was given, so `omelette-fleet set grok.outputCap=0.5` became the `0` the key exists to forbid and `maxTurns=1.9` became a `1` nobody wrote. A fraction is refused outright now — in the file, in an environment variable and through `set`, which says what it wanted (`expected a positive integer (a whole number — 0.5 and 1.9 are refused, not rounded)`). `docs/CONFIG.md` says the same for every posint key.
- **The `/omelette-test` skill's `argument-hint` is quoted.** `argument-hint: [spec path] [repo path]` is not the hint it looks like: YAML reads `[spec path]` as a flow sequence and then finds a second one stapled to it, which is a parse error — and a skill file a YAML reader rejects is a skill Claude Code never loads. It ships as `"[spec path] [repo path]"`, and the frontmatter test now refuses an unquoted value that opens a flow collection, the same way it already refused an unquoted `": "`.
- **Platforms: macOS and Linux are supported; Windows through WSL.** Native Windows is not supported yet. CI runs the suite on `windows-latest` as a **non-gating signal** — 43 tests fail there today (POSIX path assertions, fake vendor binaries with no `.cmd` shim, file-mode checks), which is harness and platform work rather than a fault in the units, and it is tracked for 0.3.3. The README's Requirements section says the same.

## 0.3.1 — 2026-09-06

0.3.0 shipped the operating model as text. 0.3.1 turns three of its rules into mechanisms, and fixes one thing 0.3.0 promised but could not deliver for Grok.

- **Grok's hard-kill salvage now has something to salvage.** `grok_research` and `grok_code_review` ask the CLI for `--output-format streaming-messages-json --include-partial-messages` instead of `--output-format json`. The reason is measured, not theoretical: the `json` format writes **nothing** to stdout until the run ends — 0 bytes at 20 s on a probe whose plain-mode twin had text by 16 s — so 0.3.0's "keep what the run produced" path found an empty buffer on exactly the runs it exists for. The streaming format emits NDJSON as the answer is produced; a 30 s kill on the 2026-09-06 probe left 441 text deltas / 1513 characters recoverable. One parser serves both paths: `text_delta` events assemble the running answer (thinking deltas and tool-argument fragments never join it) and the final whole message wins when the run got that far, being the authoritative full text; a line truncated by the kill is skipped. The partial markers are unchanged, and `[grok: hard-killed after Ns — …]` now carries real text. Image runs keep plain mode.
- **Grok reports token usage.** Taken off the same stream and merged across the events that carry it, so a final message reporting only output tokens cannot erase the input count an earlier one gave — and a reported **0** survives the merge as the count it is, rather than reading as "none given". It reaches the status feed as `usage: { input, output }`, next to Gemini's. Grok reported none before.
- **Ledger and handoff, as a rule and as a hook.** The rules file and `docs/ORCHESTRATION.md` gain a "Ledger and handoff" section: keep a ledger for every plan from the first step, one line per event, every decision as `Ruling: <what> — <why> — <cost if wrong>`, tasks marked complete with their commits; append a handoff block before a compaction and at every natural pause; re-read the ledger after a compaction before doing anything else. Location is `<project>/.omelette/ledger-<plan>.md` by convention — the rule is the file, not the path. The `PreCompact` half of the new guard hook stamps `## Compaction <ts> (trigger: <manual|auto>) — re-read this ledger before continuing` into every `.omelette/ledger-*.md`, so the marker lands even on a compaction nobody announced.
- **`omelette-fleet rules --hooks`** writes `.claude/hooks/omelette-guard.mjs` — one managed script serving both hook events — and **prints** the `hooks.PreToolUse` / `hooks.PreCompact` snippet to paste into `.claude/settings.json` or `.claude/settings.local.json`. It prints it on every run, not only the one that created the script, because a script nobody calls is the failure mode where everything looks installed; under `--dry-run` it announces the snippet instead of printing it, since a snippet naming a script that was never written is a snippet somebody pastes. The path in it is absolute and shell-quoted — a hook `command` is a command line, not an argv, so a checkout under `~/My Projects` would otherwise paste a hook that runs `node /Users/you/My`. The CLI never edits either settings file: 0.3.0's rule that Claude Code's config is read and never written now covers them too. `doctor` parses **both** of a scope's settings files read-only and reports `hooks  project: v0.3.1 (wired: PreToolUse, PreCompact)` or `NOT wired — paste the snippet from rules --hooks`, matching on the script's name inside the command string rather than on an exact path (so the quoted form still reads as wired), and naming a settings file it could not parse rather than counting it as unwired.
- **The coder's "never commits" is enforced.** The guard's `PreToolUse` handler (matcher `Bash`) exits 2 when the calling `agent_type` is `omelette-coder` and the command is a `git` invocation that creates commits, moves branches or touches stashes, worktrees or tags: `commit`, `merge`, `rebase`, `cherry-pick`, `revert`, `am`, `pull`, `push`, `stash`, `tag`, `worktree`, `branch <name>`, a `branch` flag that moves a ref (`-m`/`-M`, `-c`/`-C`, `-f`/`--force`, `-u`/`--set-upstream-to`), and any `checkout`/`switch` that creates a branch (`-b`/`-B`, `-c`/`-C`, `--create`, `--force-create`, `--orphan`, `--track` — wherever the flag sits in the command, name attached or not). Options ahead of the subcommand are stepped over with their values, quoted ones included (`-C "/My Dir"`, `-c user.name="A B"`, `--work-tree /tmp/x`, a value-less `-p`, a bare `--`), so prefixing them does not slip past. `git log`, `git diff`, `git status`, `git commit-tree` and `git branch` as a listing (`-l`, `--list`, `-a`, `-r`, `-v`, `-d`/`-D`) are deliberately untouched. It is containment for a delegated agent, not a boundary: it reads one command string, so an argv-array spawn, a backslash-escaped `g\it` or `$(which git)` is out of scope by design. It is a **settings-level** hook keyed on `agent_type` rather than a `hooks:` block in the agent's own frontmatter, because the frontmatter form did not fire at all on Claude Code 2.1.261 — no log line, `git commit` went straight through — while the settings-level form fired for the same sub-agent, blocked it, and carried `agent_type` on its stdin (spiked 2026-09-06). The script never throws: malformed stdin, an unknown event or an unwritable ledger all exit 0 — and so do stdin past 1 MiB and stdin that never closes (abandoned after 5 s), since the pipe belongs to somebody else. A `.omelette/ledger-*.md` that is not a regular file is `lstat`ed and skipped, so a symlink planted under that name is not followed and a FIFO does not hold the append open.
- **`/omelette-test <spec path>` — the tester handoff as a mechanism.** `rules --agents` now also writes `.claude/skills/omelette-test/SKILL.md`: `context: fork`, `agent: omelette-tester`, `argument-hint: [spec path]` (model-invocable on purpose: the orchestrating session calls it through the Skill tool — a `disable-model-invocation: true` key would leave only a human able to run it, which the live check on 2.1.261 confirmed), and a body carrying `` !`git diff HEAD` `` and `` !`git ls-files --others --exclude-standard` ``. Those run at invocation, before the fork, so the forked tester opens with the working tree's real diff — there is no step at which the coder's summary could be substituted for it. `$ARGUMENTS` is the spec path. The rules file's tester flow points at the skill and keeps the manual `subagent_type: omelette-tester` dispatch as the fallback.
- **Agent settings live in the fleet config.** A new top-level `agents` block — `agents.coder.model` (`opus`), `agents.coder.effort` (`xhigh`), `agents.tester.model` (`sonnet`), `agents.tester.effort` (`xhigh`), `agents.tester.maxTurns` (`80`) — is what the shipped definitions render from: the templates carry `{{model}}`, `{{effort}}` and `{{maxTurns}}`. `omelette-fleet set agents.tester.maxTurns=120` takes the dotted three-part path, `omelette-fleet show agents` prints the block with each value's source, and `rules --agents` re-renders from the config as it is at that moment (the marker stays the proof of ownership; a changed value just makes the content differ). Retuning a role is now a config change the orchestrator can make mid-plan — the rules file spells out the turn-limit case: raise it, re-render, re-dispatch. An invalid value in the file is a warning and the built-in default, never a refusal to write the definition; through `set` the same mistake is refused outright.
- **`omelette-tester` gained `maxTurns` and a line about it**: a truncated run is not a failing suite, and it must say so in its report. The template also states outright that it never modifies the implementation or the implementer's tests.
- **Managed files generalise to four kinds.** Rules, agents, skills and hooks share one registry and one `syncManagedFile`, so each kind gets the same marker-is-ownership rules, the same refresh / foreign / remove behaviour, the same `--print` and `--dry-run`, its own `doctor` line and its own `update` refresh hint. Only the comment syntax of the marker differs (`<!-- -->`, a YAML comment on line 2, `//` on line 1). A flag selects which kinds a run touches — `--remove --hooks` takes away the rules file and the marked guard script, and nothing else — and an unmarked file is never removed whichever flag named it. `--remove` never removes a directory either: `--remove --agents` takes the skill's `SKILL.md` and leaves the empty `.claude/skills/omelette-test/` for you to delete.
- **Docs.** `docs/ORCHESTRATION.md` gains "Ledger and handoff", the clean-context rationale for the tester (a second, uncorrelated context catches what the author's cannot — Boris Cherny's test-time-compute tip), a monorepo placement note, "narrow the run when the full suite is Docker-bound", the hook layer in "How the rules reach a session", and the Grok streaming note in "Supervising". `docs/CONFIG.md` documents the `agents` block and the dotted `set` form; `docs/SECURITY.md` gains a section on exactly what the guard does and restates that your settings files are never written; `docs/ADAPTERS.md` recommends a streaming output format over a whole-document one, with the measurement behind it; `docs/STATUS-FEED.md` records Grok's usage. The README gains the `rules --hooks` step and a "Fewer permission prompts" snippet, and its inline copy of `examples/fleet.config.json` — which was missing the `agents` block — now matches the file byte for byte.

## 0.3.0 — 2026-09-06

- **The operating rules reach the session by themselves.** Every unit server now returns the fleet contract from the MCP `initialize` handshake (`InitializeResult.instructions`), which Claude Code shows as "MCP Server Instructions": units propose and the session applies, absolute paths and one job per call, no unit is a source of record, and every mutation stays with Claude. It is under ten lines plus one line describing that unit, it needs no user action beyond the restart `install` already asks for, and it lives in `core/rules.mjs` next to the longer text so the two can never disagree.
- **`omelette-fleet rules [--global] [--agents] [--print] [--remove] [--force] [--dry-run]`** — writes the full operating model (tester flow, arbitration rule, routing table) to `<cwd>/.claude/rules/omelette-fleet.md`, or under `$CLAUDE_CONFIG_DIR`/`~/.claude` with `--global`, where Claude Code loads it like CLAUDE.md. Line 1 carries a version marker, and that marker is the only proof of ownership: a marked file is refreshed in place (atomically, tmp + rename) and an identical one is left alone with "up to date", while a file *without* the marker is never written over unless you pass `--force`, and is never removed at all — `--remove` refuses it and tells you to delete it by hand. `--print` and `--dry-run` touch nothing.
- **Shipped sub-agent definitions** — `rules --agents` also installs `omelette-coder` (`model: opus`, `effort: xhigh`) and `omelette-tester` (`model: sonnet`, `effort: xhigh`, tools `Read, Glob, Grep, Bash, Write, Edit`) into `.claude/agents/`. They exist because two things can only be said in a definition — a sub-agent's `effort:` (the `Agent` call carries `model` and nothing else) and `disallowedTools` — so the two roles the operating model names ship ready to use. Their ownership marker is a YAML comment on line 2 and follows the same refresh/refuse/remove rules.
- **`gpt-6-astra` is the Codex default**, at `effort: high` (operator decision 2026-09-05). Verified live on codex-cli 0.153.4 on a ChatGPT Plus plan at effort low, high, xhigh and max; `gpt-6-astra-pro`, `gpt-6-pro` and `gpt-6` are rejected on a ChatGPT plan with the same message as `gpt-5.6-sol`. It leads the catalog on the AA Intelligence Index (55 vs sol 51, terra 47) and on the vendor's cyber and long-context evals. `gpt-5.6-terra` moves to second place as the cheaper, faster step-down for sweeps and routine review — astra is ~5x its price per token and markedly slower — and its entry no longer claims to be Codex's own default, which 0.153.4 changed to astra. `examples/fleet.config.json` follows.
- **Gemini 3.1 Pro demoted** in the catalog and the docs to two niches: inputs past 1M tokens, and formal or scientific reasoning where 3.8 Flash has no published numbers — plus its role as the fleet's tie-breaker when Grok and Flash disagree. It is not the heavy-reasoning upgrade for code or agentic work: 3.8 Flash leads it 68.1 vs 46.2 on coding and 67.6 vs 40.1 on agentic lanes at roughly a third of the price.
- **Grok's hallucination note now has a post-4.6 measurement.** Artificial Analysis lists Grok 4.6 on AA-Omniscience at 48.2% accuracy / 34.3% hallucination (Index 30.5, read 2026-09-05), against 4.5's ~54% hallucination — better, and still roughly one factual answer in three wrong, so the "never a sole source" rule is unchanged. The "no post-fix measurement exists" line is retired. xAI's own model card reports its narrower internal factuality eval moving the other way (0.98% → 1.7%); different task, not comparable.
- **`doctor` and `update` report the managed files.** `doctor` gained `rules` and `agents` header lines showing each scope as `v<version>` / `absent` / `foreign` (never a fault), and `update` prints the exact `omelette-fleet rules [--global]` line when a marked file is behind the installed package. Neither ever writes those files.
- **A hard-killed run no longer loses what it produced.** All three adapters used to throw on `killed`, discarding stdout — a 15-minute `grok_code_review` that outlived `timeoutS` returned nothing at all. Now each one extracts the answer with exactly the parsing its success path uses (grok's JSON envelope or plain stdout, codex's `--json` items, agy's envelope) and, when there is text, returns it with `[<unit>: hard-killed after <N>s — treat the answer as partial; raise <unit>.timeoutS in the fleet config]` appended. A kill with nothing captured still fails loudly, and a killed run is never retried. The result carries `partial: true`, which reaches the status feed's `end` event beside `usage` while `status` stays `"ok"` — the marker in the text is the signal, and the call is still a success with an answer to read. `examples/fleet.config.json` raises grok's `timeoutS` from 900 to 1800 s after the observed 15-minute reviews.
- **Sub-agent definitions carry `disallowedTools: Agent`.** "Do not spawn sub-agents" was prose in the template body, which is a request; the denylist is applied by the harness before `tools` resolves, so the coder and the tester now *cannot* spawn one. The templates also take an inline brief (a path or the text itself) and fall back to putting the full report in the reply when no report path was given.
- **Effort precedence documented, not guessed.** `CLAUDE_CODE_EFFORT_LEVEL` beats every agent definition; otherwise the definition's `effort:` key; otherwise the sub-agent inherits the session level (code.claude.com/docs/en/model-config, sub-agents). The old "sources disagree, never rely on inheritance" wording is gone from the rules file, `docs/ORCHESTRATION.md` and `core/rules.mjs`.
- **Ownership markers are matched exactly.** `RULES_MARKER(version)` / `AGENT_MARKER(version)` in `core/rules.mjs` are the single source the templates (`{{marker}}`), the renderers, the parsers' regexes and the tests all use, and a file is ours only when its marker line matches the rendered one WHOLE — a line truncated after "managed by", one managed by someone else, or one with anything appended is not ours. The `rules` command also refuses a target that is not a regular file (a FIFO there would have parked the read forever), refuses it *before* reading, reports a failed `--remove` as one refusal line instead of a stack trace while still processing the remaining files, and cleans up its temporary file after a failure at any point between the exclusive open and the rename. `update`'s refresh hints skip a scope holding a file that is not ours (the refresh would only refuse it) and print once when the project and global paths are the same file.
- **Packaging** — `rules/` and `agents/` ship in the tarball; `docs/superpowers` is excluded from it.

## 0.2.0 — 2026-09-03

- **`codex_image`** — image generation on the codex unit via the Codex CLI's built-in **gpt-image-2** tool (verified live on codex-cli 0.153.0, ChatGPT plan: present headless under `--ignore-user-config --ignore-rules`, behind no feature flag). Returns the absolute path of the saved PNG, reports real token usage, and is never retried — a re-issued generation bills image quota twice. The run is spawned `-s workspace-write -C <a fresh directory under the OS temp dir>` **without consulting the fleet ceiling**: the kernel scopes every write to that one throwaway directory, outside every project, because the built-in tool saves under `~/.codex/generated_images/` and then copies the file into the working directory with a shell command. Same posture as `gemini_image`'s temp cwd; the rationale is in [docs/SECURITY.md](docs/SECURITY.md). Web search is off for image runs and no reasoning effort is passed.
- **`gemini_image` prompt hardening** — the prompt now states that only the built-in image tool may be used and that terminal commands are unavailable, mirroring the research preamble. The first live image call was lost to the model reaching for the shell `command` tool, which headless agy auto-denies; it only succeeded on the retry.
- **Update mechanism** — `omelette-fleet update` reports the latest release and fast-forwards a git checkout (`git pull --ff-only`); a dirty tree or a diverged branch is refused with the reason and nothing is written, an npm install gets the exact `npm i -g` line, and MCP registrations are never rewritten (they hold absolute paths a pull does not move). `update --check` fetches, pulls nothing, and exits **3** when an update is available so a script can branch on it. Each unit server also prints one stderr line at startup when a newer release exists: fire-and-forget, capped at 2.5 s, cached for 24 h in `<home>/update-check.json`, and silent when you are current. Opt out with `OMELETTE_UPDATE_CHECK=0` or the new top-level `"updateCheck": false` config key. The only outbound request this package makes is that one unauthenticated GitHub releases call — no telemetry, nothing downloaded, nothing executed (see [docs/SECURITY.md](docs/SECURITY.md#network)).
- **`doctor`** gained a `version <current> · latest <latest>` header line, and `--help` covers the new command.
- **`gemini_image` is no longer retried.** With the prompt fixed, a second attempt only spends image quota twice — the same rule the fleet's other image tools already followed.
- **`serverInfo.version`** now comes from `package.json` instead of a constant frozen into `core/unit.mjs`, so `initialize` stops reporting a stale release number.
- **`core/artifact.mjs`** — `extractImagePath()` lifted out of the grok adapter and shared with codex: an image tool returns a path that exists on disk, never one the model asserted.

## 0.1.0 — 2026-09-03

Initial release.

- **Three units**, each a standalone stdio MCP server spawning a vendor CLI headless on the subscription you already pay for:
  - **gemini** (`agy`) — `gemini_research`, `gemini_deep_research`, `gemini_image`, `gemini_models`.
  - **grok** (`grok`) — `grok_research`, `grok_code_review`, `grok_image`, `grok_image_edit`, `grok_models`.
  - **codex** (`codex`) — `codex_research`, `codex_code_review`, `codex_models`.
- **Write ceiling** — `workspace-write` takes two keys: the unit's `mode` in the config file *and* the unit named in `OMELETTE_ALLOW_WRITE` in the server environment (`ORION_ALLOW_GEMINI_MUTATE=1` honoured as a legacy alias for `gemini`). The config can only narrow, and a unit that does not implement a mode refuses it even with the ceiling open.
- **Environment allowlist** — a vendor CLI's environment is built from a fixed allowlist plus the unit's `envPassthrough` patterns plus `OMELETTE_ENV_PASSTHROUGH`, then the billing scrub, then the adapter's own additions. Nothing is inherited: a model running read-only shell commands cannot read tokens or cloud credentials out of the server's shell.
- **Codex config isolation** — every run passes `--ignore-user-config --ignore-rules`, so `~/.codex/config.toml` (MCP servers, plugins, hooks, notify) never reaches a fleet call; the OS sandbox bounds the filesystem, not a configured MCP tool. With no model configured the adapter pins the catalog head explicitly.
- **Read-only by default**, enforced per vendor: Codex's OS-level sandbox, Grok's six-layer spawn-arg toolset allowlist (write mode declared unsupported), agy's permission policy plus `--disable-slash-commands`.
- **Fleet config** at `$OMELETTE_HOME/fleet.config.json` (default `~/.omelette`): built-in → `defaults` → `units.<unit>` → environment, re-read per call, malformed files degrade to a warning.
- **Status feed** (schema 1): per-unit snapshot plus a shared NDJSON log, atomic, 0600, self-trimming, fail-soft.
- **Bounded spawn**: own process group with a wall-clock SIGKILL, output caps, actionable missing-binary errors; a 16 MiB cap on one JSON-RPC frame keeps a broken client from taking the server down.
- **Honest results**: a run that produced text but exited badly is returned with a visible partial marker, and adapter refusals come back as MCP errors rather than error-shaped successes.
- **Model catalogs** per unit — allowlists validated before any spawn, ids and effort levels verified live, with routing guidance served by each `<unit>_models` tool.
- **`omelette-fleet` CLI**: `install`, `uninstall`, `doctor [--probe-models]`, `show`, `set`, `call`, each answering its own `--help`. Never shells out, and writes nothing but the fleet config. `doctor` reads Claude Code's config from `CLAUDE_CONFIG_DIR` before `~/` and says which, counts a registration as yours only when it points at this clone's server file, and reports an unreadable probe as `unknown` rather than guessing; `uninstall` tells a no-op apart from a failed removal; `call` rejects non-object arguments and treats a protocol-level error as a failure instead of an empty success.
- Zero runtime dependencies; Node ≥ 20; `node --test` invariant specs under `test/`.
